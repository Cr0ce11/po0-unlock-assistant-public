# 项目待办

本文件是项目的可执行清单。状态变化通过 Pull Request 更新，不能只在聊天中标记完成。

状态说明：`进行中`、`待办`、`待决定`、`暂缓`、`阻塞`、`完成`。完成项在对应 Release 或治理 PR 合并后移入 [`CHANGELOG.md`](CHANGELOG.md) 或从本文件删除，不长期堆积。

本仓库没有启用 GitHub Issue（项目所有者 2026-08-17 决定不启用，见 GOV-002），因此每个事项的“正式记录”指向 Pull Request、Release 或仓库内的长期文档。

| 编号 | 优先级 | 状态 | 正式记录 | 事项 | 完成条件 |
|---|---|---|---|---|---|
| GOV-001 | P1 | 完成 | [PR #6](https://github.com/Cr0ce11/po0-unlock-assistant-public/pull/6) | 建立项目状态、待办与维护交接基线 | 三份长期文档与 v2.5.18、公开 CI 和 Release 事实一致；不改变运行代码、生成物或服务器行为，本地验收清单与公开 CI 全部通过 |
| HAND-001 | P1 | 完成 | [`docs/HANDOVER.md`](docs/HANDOVER.md) | Claude Code 维护期（2026-08-14 至 2026-08-21） | 2026-08-21 项目所有者决定交回 Codex；交回记录已列明维护期 PR、Release、生产操作、风险、回退方式、交回基线与开放项，`PROJECT.md` 同步改为 Codex 执行、Claude 独立复核 |
| BUG-020 | P1 | 完成 | 本文件（2026-08-21 Codex 接管审查） | 完整回滚忽略国外出口两个核心服务的停止失败 | 已修：国外出口完整回滚新增统一停止闸门；仍存在 unit 文件时，两个核心服务必须各自成功执行停用，并通过 systemd 最终状态确认进入 `inactive` 或 `failed`，之后才允许删除 unit、配置、密钥与主机记录。停用失败、状态无法确认或仍在运行时立即中止，国外出口全部材料与 `ACTIVE` 状态保持不动，并保留 systemd 的实际错误供排查；国内入口 Agent 回滚已经在此前完成。上一轮已删除 unit、却在最终封存 ACTIVE 时失败的重试路径会跳过无意义的停用命令，但仍复核最终活动状态。运行时故障注入验收覆盖两项停用命令失败、命令成功但服务仍活动、状态无法确认、`failed` 视为已停止、unit 已删除后的重试，以及修正后的成功清理。**同类写法核对结论保持不变**：`repair_service_state` 中的 `disable`／`stop ... || true` 是安全修复失败后的第一层尽力恢复，函数随后返回失败，外层还会调用检查结果的 `restore_service_state`；该路径不删除 unit、配置或密钥，不属于本缺陷 |
| BUG-021 | P1 | 完成 | 本文件（2026-08-21 Codex 修复并按审查补强） | cf-probe forward 模式的启用、刷新和拆除不是完整失败事务 | 已把转发三件套及 systemd 启用／运行状态纳入事务快照：复用既有完整状态时，写文件、重新载入或启动失败都会恢复原状态，且内外层共用一份快照；上次中断留下的残缺受管状态不再被快照预检永久堵死，确认归属后可撤销或直接清理重建。刷新在最终提交前失败会恢复原 unit 与服务状态；撤销必须确认转发单元已经停止，失败时保留全部材料和托管记录。运行时验收覆盖 hosts／record 写入失败、unit 写入失败、复用启动失败、残缺状态撤销与重建、刷新 reload／restart／enable 失败、撤销停用失败与外层事务回滚；全部路径均验证无孤儿监听、无错误注销且可直接重试 |
| SEC-006 | P2 | 完成 | 本文件（2026-08-21 Codex 修复并按审查补强） | cf-probe forward 受管文件的归属与内容校验弱于项目其他组件 | 复用、刷新和快照已收紧到严格状态：目录及三类文件必须保持 root:root 与精确权限，record 的服务名、面板主机、监听端点逐项验证，新记录绑定 hosts SHA-256，unit 只接受当前模板或已知旧模板；非法字段、换序／格式变化、权限漂移、内容夹带和摘要不符都会在调用 systemd 前拒绝且不覆盖。撤销单独使用最小归属判断，只删除 root:root、非符号链接、单硬链接且带 Po0 标记的文件，所以归属明确的权限／内容漂移和残缺状态仍可安全解管，外来文件继续拒绝。旧四行 record 与旧 unit 经精确旧格式校验后仍可刷新迁移。生产 `/etc/hosts` 路径固定，不接受环境覆盖。运行时验收逐个覆盖四类权限漂移、hosts／record／主机名／服务名／监听端点／unit 内容篡改、同样行数的 record 换序与格式漂移，并验证严格复用拒绝后仍可撤销 |
| BUG-022 | P3 | 待办 | 本文件（2026-08-21 Codex 接管审查） | 旧 Shell 版 cf-probe 会被菜单提供不支持的 forward 选项 | 扫描与管理菜单只按服务身份判断 CF Probe，选择 forward 后底层却只接受 Go 版配置，因此旧 Shell Agent 会先被允许选择、再以“无法定位配置文件”失败。失败路径会完整回滚且没有残留，实际影响只是一次无效菜单交互，故定为 P3。修复条件：展示选项前区分实现；旧 Shell 版只提供 env，或在确认前明确说明并拒绝；增加旧 Shell 夹具和运行时验收 |
| BUG-023 | P2 | 待办 | 本文件（2026-08-21 Codex 接管审查） | 非交互 `po0 check` 没有可预测的总体时限 | 现有控制主连接用 `ServerAliveInterval=15`、`ServerAliveCountMax=4` 探测失联，每次新建 SSH 连接也有 10 秒连接上限，健康组件执行本身另有 90 秒上限；缺口不是完全没有局部超时，而是 `preflight`、复用控制连接、组件选择/上传及国外出口检查没有共享的总预算，定时巡检的最坏耗时不可预测。修复条件：为整个只读检查建立可预测的总上限，超时返回退出码 3 和可诊断原因，退出时仍清理临时组件、控制连接和操作锁；增加真实挂起夹具验收 |
| DOC-002 | P3 | 待办 | 本文件（2026-08-21 Codex 接管审查） | `TODO.md` 长期堆积完成项 | 文件开头规定完成项应移入 `CHANGELOG.md` 或删除，但当前仍保留大量完成行。修复条件：在不丢失长期结论的前提下把完成事实归并到 CHANGELOG/交接文档，只保留开放项和仍需重新评估的决定 |
| FEAT-001 | P2 | 完成 | [PR #35](https://github.com/Cr0ce11/po0-unlock-assistant-public/pull/35) | 为定时巡检和手机端助手提供健康检查的机器可读入口 | 已完成：新增 `po0 check` 与 `po0 check --json`，退出码 `0/1/2/3` 区分正常／提醒／异常／检查失败，`detail` 沿用诊断报告脱敏规则，不依赖终端、不改配置或服务、不建恢复点，也不参与已安装脚本接管；现有菜单与既有子命令行为逐字节不变。需求原写命令名为 `status`，因该名已被现有健康检查占用且需求同时要求不改既有子命令行为，改名为 `check`。新增 4 项验收（含「总体判定与逐项等级一一对应」的假绿灯守卫），本地验收清单与公开 CI 全部通过 |
| FEAT-002 | P2 | 完成 | 本文件（2026-08-19 需求：cf-probe 出站模式可选） | 为 cf-probe 提供可选的出站模式：env（现状）与 forward（本地转发） | 第一步已完成：国内入口组件支持 `enable-service <unit> [env\|forward]`，forward 模式建立受管的转发目录、专属 hosts、受管记录与转发单元，并在 drop-in 里以 `BindReadOnlyPaths` 只对该服务生效；启用失败整套回滚、停用与完整回滚一并拆除、刷新会重新写回挂载行、健康检查区分正常/异常/提醒。护栏：缺 socat、监听端点被占、面板地址无法解析、同名单元非本助手所写，四种情况一律拒绝且不留残留；forward 仅接受 cf-probe。默认仍是 env，既有部署行为逐字节不变。第二步已完成：Agent 扫描在配置 CF Probe 时询问出站方式，已托管的 CF Probe 在管理菜单里新增「切换出站方式」，切换复用「先撤销再重新纳管」两条既有事务路径；撤销成功而重新纳管失败时明确告知服务当前是直接联网状态并提示重新扫描。新增两条运行时验收（选择菜单取值与拒绝、切换的正常/取消/两种失败路径）与两条接线断言，变异验证均被抓出。**真机确认（2026-08-19，v2.5.28）**：项目所有者在生产国内入口上用管理菜单把 cf-probe 切到 forward 出站方式，纳管成功；实时通道日志出现 `WSS connected`（面板地址按安全红线不写入仓库），与 HTTP 上报的代理连接并存；四项测速目标同时被改写为国内直连 TCP 53 节点。此前为验证方案而手工放置的转发件已全部删除，现由 Po0 托管 |
| GOV-002 | P2 | 完成 | 项目所有者决定（2026-08-17） | 是否启用 GitHub Issue 作为需求与缺陷的登记入口 | 已决定**不启用**。需求、缺陷与优先级继续由本文件配合 Pull Request、Release 和仓库内长期文档承载，不再把「启用 Issue」当作待决定项；今后若所有者改变决定，再重新立项并同步调整 `PROJECT.md` 与本文件 |
| GOV-003 | P3 | 完成 | 项目所有者决定（2026-08-17） | 是否授予开源许可 | 已决定**授予 GPL-3.0-only**，版权署名 `Cr0ce11`。新增 `LICENSE`（gnu.org 规范版 674 行纯文本，sha256 `3972dc97…36986`，与 SPDX 版逐词比对一致，差异仅为 `(C)`/`©` 与引号排版），同步更新 `README.md` 的仓库与许可说明、`PROJECT.md` 的仓库形态与决定权、`AGENTS.md` 的公开仓库边界。仓库内无第三方代码，CI 只引用官方 Action 且不进产物，因此整体授权无需第三方许可声明。未加逐文件许可头：那会改动生成物且不是 GPL 的必要条件，如今后需要再单独立项 |
| OPS-001 | P2 | 完成 | 项目所有者决定（2026-08-17 关闭） | v2.5.18 的专门真机验证 | 已关闭：针对 v2.5.18 的专门验证被后续版本的真机验证取代，不再单独安排。当时的决定是不设临时验证机、由所有者在实际使用中验证，实际也按此办理——BUG-012、BUG-014 均由使用反馈发现并当日修复发布；v2.5.22 的扫描路径由所有者确认恢复正常；v2.5.22 与 v2.5.24 的验证结果分别记录在 [PR #30](https://github.com/Cr0ce11/po0-unlock-assistant-public/pull/30) 与 [PR #37](https://github.com/Cr0ce11/po0-unlock-assistant-public/pull/37)。真机验证不作为常设流程写入仓库（HAND-001：不给接手方新增流程要求）；今后若要恢复专门验证，先记录授权方式、检查项和失败恢复路径，且不把地址、端口或凭据写入仓库 |
| SEC-001 | P1 | 完成 | 项目所有者决定（2026-08-17 定案） | 处置改名后被释放的旧账号名 `DTB201` | 已定案**不注册占位账号**。风险面：GitHub 账号改名后旧名可被任何人重新注册，一旦被他人注册，v2.5.18 及更早版本内置的旧更新地址会指向他人仓库。缓解事实：v2.5.19 起更新器明确拒绝改名前账号下的仓库地址（验收 `test_updater_rejects_previous_owner_repository` 守着这条），因此只有仍停留在 v2.5.18 及更早的安装受影响，而所有者的生产机运行的是 v2.5.24。所有者据此判断个人自用、影响可接受，定案关闭。今后若有他人使用本项目、或发现仍在运行 v2.5.18 及更早版本的安装，应重新评估 |
| REL-013 | P1 | 完成 | v2.5.30 Release | 发布健康检查转发模式判定修复版本 | v2.5.30 已发布（2026-08-19）；标签与脚本内版本一致（`SCRIPT_VERSION=2.5.30`），Release 为正式版而非草稿或预发布。核对结果：主附件 `po0-unlock-v2.sh` 的 GitHub digest `sha256:41e53ab8…da7202` 与本地产物 SHA-256 一致；`.sha256` 附件内容与主附件实际摘要逐字符一致，其自身 digest `sha256:59de1466…988673` 也与本地计算一致；下载回来的发布产物与本地产物逐字节相同，且 `self-test` 通过。本版修复 BUG-019，让健康检查里转发模式的四项判断真正生效。**验收信号**：更新后在国外出口机上跑 `po0 check`，已启用转发模式的 cf-probe 那一项说明里应带「（转发模式）」字样；仍是光秃秃的「配置完整且正在运行」则说明没生效。**真机验证已完成（2026-08-19）**：生产国外出口机更新到 v2.5.30 后，`po0 check` 里已启用转发模式的 cf-probe 那一项显示「配置完整且正在运行（转发模式）」，而同一份输出里未启用转发模式的另一个 Agent 仍是通用措辞——两者的差异本身就是这条分支真正走到了的证据。随后经项目所有者授权做了一次故障注入演练：在国内入口上停掉转发单元后，`po0 check` 退出码为 2、整体判定为异常，且**唯一变红的就是该 Agent**（说明为「转发单元没有运行，实时通道已中断」），其余 23 项全部保持正常；随即恢复转发单元，监听端口回来、单元恢复 active/enabled、全机失败单元数为 0、无残留，`po0 check` 回到 24/24 正常。演练期间探针的实时通道断开约 61 秒后自愈，其间自动回退到 HTTP 上报，与 OPS-004 记录的 `auto` 模式行为一致，面板未掉线 |
| BUG-019 | P1 | 完成 | 本文件（2026-08-19 发布后核对） | 健康检查的转发模式判定跨作用域调用，自 v2.5.26 起从未生效 | 发现于 v2.5.29 发布后的真机核对：项目所有者按提示跑完「检查并更新配置」，转发单元确实补上了开机自启，但 `po0 check` 里那项显示「配置完整且正在运行」，少了转发模式该有的「（转发模式）」字样。查明根因：`cf_probe_forward_mode_active` 与 `cf_probe_forward_health` 只定义在国内入口组件里，而健康检查是从角色脚本调用它们的——组件是被写到对端执行的独立脚本，角色脚本取不到其中的函数，运行时报 `command not found`，`if` 条件为假，于是永远走「一切正常」那条分支。这是 BUG-016 的同类错误、方向相反。**影响**：自 v2.5.26 引入转发模式起，健康检查里转发模式的四项判断从未生效——转发单元没运行（应报异常「实时通道已中断」）、专属 hosts 被改（异常）、与系统 hosts 脱节（提醒）、不会开机自启（提醒，v2.5.29 新增）——一律显示「配置完整且正在运行」。转发模式本身、启用、刷新、停用与完整回滚都不受影响，坏的只是「出事了会告诉你」这一半，因此是健康检查自身的假绿灯。全仓库审计确认受影响的只有这一处调用点、这两个函数。已修：组件新增只读入口 `cf-probe-forward-health <unit>`（不取锁、不改状态，退出码 0 表示给出结论、3 表示该服务未启用转发模式、其余表示无法判定），角色侧的健康检查经它取结论；无法判定时如实报「无法确认转发模式状态」并计为异常，绝不退回「看起来正常」。**为什么以前没抓到**：既有验收直接调用了那两个函数，证明函数本身写得对，却没有验证它们能从健康检查这条路真正走到；而作用域守卫只有「组件→角色」一个方向。已补：反方向的作用域守卫、一条从健康检查入口实跑的用例（覆盖正常／提醒／异常／组件失败四种结论及其对整体判定的影响）、一条按真脚本执行组件只读入口的退出码用例，以及两条接线断言。四处变异——角色侧退回直接调用、组件入口去掉、未启用转发模式的退出码改错、组件失败时退回「正常」——均被立即抓出，其中第一处的用例日志里原样出现了生产上那句 `command not found` |
| REL-012 | P1 | 完成 | v2.5.29 Release | 发布转发单元开机自启修复版本 | v2.5.29 已发布（2026-08-19）；标签与脚本内版本一致（`SCRIPT_VERSION=2.5.29`），Release 为正式版而非草稿或预发布。核对结果：主附件 `po0-unlock-v2.sh` 的 GitHub digest `sha256:415035af…28d952` 与本地产物 SHA-256 一致；`.sha256` 附件内容与主附件实际摘要逐字符一致，其自身 digest `sha256:de4c9099…2b6bb6` 也与本地计算一致；下载回来的发布产物与本地产物逐字节相同，且 `self-test` 通过。本版修复 BUG-018，让 cf-probe 转发单元真正开机自启，并让「检查并更新配置」把既有部署的转发单元收敛到当前版本。**更新后需要执行一次「检查并更新配置」**，已落地的旧单元才会被重写；在此之前健康检查会以「提醒」持续指出该部署重启后实时通道会断。真机验证由项目所有者更新后在实际使用中进行 |
| SEC-005 | P2 | 完成 | 本文件（2026-08-19 项目所有者决定） | 清除仓库内记录的生产面板真实域名，并定下历史不重写的取舍 | 2026-08-19 维护交接核对时发现 FEAT-002 行里贴的一条真机日志含生产面板的真实域名，违反 `AGENTS.md` 安全红线「不得把真实密码、令牌、SSH 私钥、公钥、生产 `known_hosts` 内容、真实服务器地址或端口写入代码、测试夹具、提交信息或本文件」，且该行已随 PR #55 进入公开仓库的 `main`。项目所有者决定的处置：**当前文件中的域名删除，提交历史不重写**。取舍本身：域名不是凭据，公开 DNS 即可解析，泄露的是「面板在哪、归谁」这类指向信息而非访问能力；而重写公开仓库的历史会改掉 v2.5.17 以来全部提交的哈希，使每个已发布版本标签与其提交的对应关系失效，代价远大于收益。**哪天不再接受这个取舍**——例如该域名被用于定向探测，或仓库要作为对外样板被长期引用——退路是更换面板域名而不是重写历史：换掉之后历史里那条记录自然失效。**同时约定**：今后往仓库写真机证据时，日志、命令输出与链接一律先去掉主机名、IP 和端口，只保留能说明问题的字段 |
| BUG-018 | P1 | 完成 | 本文件（2026-08-19 维护交接核对） | cf-probe 转发单元没有开机自启，国内入口重启后实时通道静默断开 | 交接核对时在生产国内入口上实读发现：转发模式当时运行正常，但转发单元文件没有 `[Install]` 段，`systemctl is-enabled` 返回 `static`，各 `.wants` 目录里没有它的链接，cf-probe 自身的 `Requires`／`Wants`／`After`／`BindsTo` 里也没有任何本助手的单元——没有任何东西会在开机时拉起转发器。后果远不止提示噪音：入口机一旦重启，转发器不启动，探针的实时通道打到本机监听端口被拒而中断，HTTP 上报却照常经代理送达，面板继续显示在线，正是转发模式本来要消灭的那种假绿灯。安装时 systemd 打的那句「unit files have no installation config」不是噪音，正是这个缺陷的症状。已修：① 单元声明 `WantedBy=multi-user.target`，`systemctl enable` 从空转变成真正生效；② 刷新（「检查并更新配置」）新增转发单元收敛——内容与当前期望不一致就重写、重新载入 systemd 并重启正在运行的转发器，内容一致时也重新启用一次以补回缺失的开机链接，既有部署因此不必重新纳管；单元不是本助手所写一律拒绝；③ 健康检查新增「转发单元不会开机自启」提醒。选择给单元加 `[Install]` 而不是在 drop-in 里加 `Wants=`＋`After=`，是因为后者要改受管 drop-in 的内容，会牵动归属校验、刷新写回与完整回滚整条链路，不符合「改动小而单一」。新增一条运行时验收（新单元带 `[Install]` 且被启用、旧单元被重写并重新启用、内容一致时不重写但仍启用、他人单元被拒绝）、一条刷新接线断言，健康检查提醒另配对照断言（只补上开机自启就必须回到正常）。四处变异——去掉 `[Install]`、去掉刷新接线、去掉健康检查、去掉收敛后的启用——均被立即抓出 |
| OPS-004 | P3 | 完成 | 本文件（2026-08-19 核对） | 记录 cf-probe 上报通道与实时通道的既定行为，避免重复排查 | 两条结论落进正式记录，都在交接核对时查实：① **探针只有一条连接是正常稳态**。官方 Go 版 cf-probe v1.0.9 的 `connection_mode` 默认为 `auto`，其源码里发出 HTTP 上报的条件是「实时通道未连接」，因此实时通道一旦连上就完全不走 HTTP，断开才回退。生产上 30 秒六次采样只见到实时通道那一条连接，而代理日志里唯一一次由探针自己发起的连接正好落在实时通道断开的那一分钟内，与源码一致。`PROJECT.md` 里「HTTP 上报与实时通道并存」是切换当时的瞬时观测、不是稳态，已同步更正。② **实时通道每隔一段时间以 `reason=EOF` 断开重连（约 1 分钟自愈）是面板侧的正常连接回收**：项目所有者说明该面板在无人打开时进入休眠；直连的国外出口机上同样如此。两者都不是缺陷，不再排查。**留给所有者确认的一点**：auto 模式只在实时通道「断开」时才回退 HTTP，若面板休眠期间连接仍挂着，这段时间的数据可能既没进面板、也不会被 HTTP 补发；这只能从面板侧看曲线有无空档，探针侧看不出来 |
| REL-011 | P1 | 完成 | v2.5.28 Release | 发布白名单粒度修复与 v1.0.9 入清单版本 | v2.5.28 已发布（2026-08-19）；标签与脚本内版本一致（`SCRIPT_VERSION=2.5.28`），Release 为正式版而非草稿或预发布。核对结果：主附件 GitHub digest `sha256:dfd66d67…f3047` 与本地产物 SHA-256 一致；`.sha256` 附件内容与主附件实际摘要逐字符一致，其自身 digest `sha256:6ca0611d…2a47c9` 也与本地计算一致；下载回来的发布产物与本地产物逐字节相同，且 `self-test` 通过。本版恢复了因 BUG-017 而失效的 cf-probe 纳管，并让 v1.0.9 的测速目标处理重新生效；真机验证由项目所有者更新后在实际使用中进行 |
| OPS-003 | P2 | 完成 | 本文件（2026-08-19 审查） | 审查官方 Go 版 cf-probe v1.0.9 并决定是否加入白名单 | 起因：所有者机器上的 cf-probe 已由上游自动更新到 v1.0.9，超出清单范围（最新只到 v1.0.6）。按项目既有标准「官方源码与生产二进制核对」执行：① 生产二进制内嵌 `vcs.revision=69f5e132…302187`，与上游 `v1.0.9` 标签指向的提交逐字符一致，且 `vcs.modified=false`；② 上游该标签为正式 Release（`prerelease=false`），非快照；③ 复核 v1.0.6→v1.0.9 的 26 个文件改动，连接配置仍是 `KEY="value"` 形式、四项测速目标字段的读写逻辑未变，涉及配置写入与目标字段的删除行数为 0；④ 新增 `CONNECTION_MODE` 键不会被本助手弄丢——写回逻辑只替换四行、其余原样输出，且写回后用 `cmp` 逐行核对除这四行外完全一致；⑤ 上游 `configSchemaVersion` 由 3 升到 5，本助手不依赖该字段；⑥ 生产二进制仍带全部识别标记。结论：加入清单。新增验收夹具与断言，变异验证（从清单移除该修订）立即失败。**未加入 v1.0.7 与 v1.0.8**：二者同为上游正式版，但没有对应的生产二进制可核对，不满足既有标准；今后若有机器停在这两个版本，再按同一流程补审。**长期风险**：cf-probe 默认开启自动更新，上游每发一版都会重复此过程；BUG-017 的修复已让未纳入清单的版本降级为「只跳过测速目标处理」而非整次拒绝，因此不再造成失联 |
| BUG-017 | P1 | 完成 | 本文件（2026-08-19 使用反馈） | cf-probe 版本白名单挡住国外出口本身，自动更新后探针无法纳管且退回直连 | 所有者修好 BUG-016 后再次切换 cf-probe 出站方式，撤销成功但重新纳管失败：`当前官方 Go cf-probe 未在 Po0 已审查的正式版本清单中，已停止配置`。原因是该机的 cf-probe 已由上游自动更新到 v1.0.9，而白名单最新只到 v1.0.6。后果比拒绝配置严重得多——撤销已经完成，服务因此停在**未纳管、直连**状态，而它在受限网络里，直连完全不通，监控当场失联。根因是拒绝粒度：`prepare_cf_probe_latency_compat` 对「官方 Go 版但修订不在白名单」直接拒绝整次配置，而代理环境变量与测速目标处理本是两件事；`cf_probe_go_config_path` 也带同一道闸门，导致连只读取面板地址都做不到，转发模式一并失效。已修：未知修订改为返回可跳过状态（沿用 Komari 身份守卫既有的返回码 3 约定），启用与刷新两处调用点跳过测速目标处理后继续配置国外出口，并说明跳过了什么、版本进入清单后如何补上；配置路径解析拆成两个函数，只读取值不再要求修订在白名单内，改写仍然必须过白名单。「版本受支持但安全校验失败」仍整次拒绝，未放宽。新增／更新验收：未知修订必须返回 3 且给出两条说明、受支持版本的安全校验失败必须仍返回 1、启用与刷新两处都必须处理可跳过返回码。变异验证三处均被抓出 |
| REL-010 | P1 | 完成 | v2.5.27 Release | 发布国内入口组件写锁修复版本 | v2.5.27 已发布（2026-08-19）；标签与脚本内版本一致（`SCRIPT_VERSION=2.5.27`），Release 为正式版而非草稿或预发布。核对结果：主附件 `po0-unlock-v2.sh` 的 GitHub digest `sha256:52f95889…818d75` 与本地产物 SHA-256 一致；`.sha256` 附件内容与主附件实际摘要逐字符一致，其自身 digest `sha256:86d81a07…89c246` 也与本地计算一致；下载回来的发布产物与本地产物逐字节相同，且 `self-test` 通过。本版修复 BUG-016，恢复了自 v2.5.18 起失效的三条 Agent 写路径；真机验证由项目所有者更新后在实际使用中进行 |
| BUG-016 | P1 | 完成 | 本文件（2026-08-19 使用反馈） | 国内入口组件的写锁函数只定义在角色侧，三条写路径一调用就失败 | 所有者用 v2.5.26 的新菜单切换 cf-probe 出站方式时，对端报 `/usr/local/bin/po0-cn-entry: line 1631: acquire_state_mutation_lock: command not found`，切换在撤销这一步失败（失败是干净的：服务仍在运行、仍被纳管、drop-in 完好）。根因：`acquire_state_mutation_lock` 与 `CN_ENTRY_LOCK_WAIT_SECONDS` 都只在 `00-runtime.sh.inc` 定义，而它在组件 heredoc 之外；组件是独立脚本，取不到角色侧的函数与变量。受影响的是 `enable-service`、`refresh-service`（两处）、`disable-service` 四个调用点，对应菜单里的「配置新 Agent」「检查并更新配置」「撤销国外出口配置」，**自 PR #4（v2.5.18 期）起一直失败**；已配置的 Agent 正常工作，健康检查、状态查询与完整回滚不受影响，所以只有真正增删 Agent 时才会暴露——所有者上次纳管是 2026-08-13，此后未再增删，故六个版本无人发现。已修：组件自带 `acquire_helper_state_mutation_lock`（沿用项目既有的「两边各一份、名字不同」约定，如 `active_state`／`helper_active_state`）与同值的 `CN_ENTRY_LOCK_WAIT_SECONDS=30`，语义与角色侧一致。新增三条验收：两条作用域守卫（组件不得调用只在角色侧定义的函数、不得读取自己没赋值且无默认值的常量）与一条运行时用例（在组件作用域内真正取一次写锁并核对 30 秒有界等待）。变异验证：还原任一处缺陷，对应守卫立即失败，且函数缺陷同时被静态守卫与运行时用例双双抓出 |
| REL-009 | P1 | 完成 | v2.5.26 Release | 发布 cf-probe 出站方式可选版本 | v2.5.26 已发布（2026-08-19）；标签与脚本内版本一致（`SCRIPT_VERSION=2.5.26`），Release 为正式版而非草稿或预发布。核对结果：主附件 `po0-unlock-v2.sh` 的 GitHub digest `sha256:12498eb6…49934` 与本地产物 SHA-256 一致；`.sha256` 附件内容与主附件实际摘要逐字符一致，其自身 digest `sha256:e02abf39…45ea1` 也与本地计算一致；下载回来的发布产物与本地产物逐字节相同，且 `self-test` 通过。本次未安排真机验证：项目所有者决定自行更新到该版本后在实际使用中验证 forward 模式，验证结果按需另行记录 |
| REL-008 | P1 | 完成 | v2.5.25 Release | 发布许可证授予与撤销事务修复版本 | v2.5.25 已发布（2026-08-17）；标签与脚本内版本一致（`SCRIPT_VERSION=2.5.25`），Release 为正式版而非草稿或预发布。核对结果：主附件 `po0-unlock-v2.sh` 的 GitHub digest `sha256:7f584ba9…e98099` 与本地产物 SHA-256 一致；`.sha256` 附件内容与主附件实际摘要逐字符一致，其自身 digest `sha256:a02a7bde…a268fa` 也与本地计算一致；下载回来的发布产物与本地产物逐字节相同，且 `self-test` 通过。本次未安排真机验证：三项改动中 GPL 授权不涉及运行行为，QA-006 在 Debian（bash 5）上行为逐字节不变，BUG-015 只在「撤销更新到 v2.4 以下版本且中途失败」时触发，只读检查覆盖不到这些路径 |
| REL-007 | P1 | 完成 | v2.5.24 Release | 发布非交互只读入口版本 | v2.5.24 已发布；标签与脚本内版本一致，两个 Release 附件的 GitHub digest 与本地 SHA-256 逐一核对通过，`.sha256` 附件内容与主附件实际摘要一致，下载的发布产物与本地产物逐字节相同且自检通过。真机只读验证：用发布产物的候选副本在生产出口机运行 `check` 与 `check --json`，结论「正常」、退出码 `0`、24 项检查全部正常、JSON 经 `jq` 校验合法；已安装脚本 SHA-256 前后不变，验证后临时文件零残留 |
| REL-006 | P1 | 完成 | v2.5.23 Release | 发布 SEC-004 收紧版本 | v2.5.23 已发布；更新连接配置换入口时拒绝已被占用的入口，标签与脚本内版本一致，两个 Release 附件的 GitHub digest 与本地 SHA-256 逐一核对通过 |
| REL-005 | P1 | 完成 | v2.5.22 Release | 发布扫描载荷回归修复版本 | v2.5.22 已发布；修复 v2.5.21 的 Agent 扫描失败（BUG-014），标签与脚本内版本一致，两个 Release 附件的 GitHub digest 与本地 SHA-256 逐一核对通过 |
| REL-004 | P1 | 完成 | v2.5.21 Release | 发布 EXIT 陷阱修正版本 | v2.5.21 已发布；修复 v2.5.20 的陷阱串接回归（BUG-012）与内置组件临时目录残留（BUG-013），标签与脚本内版本一致，两个 Release 附件的 GitHub digest 与本地 SHA-256 逐一核对通过 |
| REL-003 | P1 | 完成 | v2.5.20 Release | 发布包含审查修复的正式版 | v2.5.20 已发布；17 条改动覆盖 2026-08-14 全项目审查确认的全部 P1／P2／P3 可执行项，标签与脚本内版本一致，两个 Release 附件的 GitHub digest 与本地 SHA-256 逐一核对通过 |
| REL-002 | P1 | 完成 | v2.5.19 Release | 发布包含新更新地址的正式版 | v2.5.19 已发布；标签与脚本内版本一致，两个 Release 附件的 GitHub digest 与本地 SHA-256 逐一核对通过 |
| BUG-001 | P1 | 完成 | 本文件（2026-08-14 审查） | 更新与撤销更新的恢复点指针先于脚本替换落盘，失败后不回滚 | 已修：新增 `snapshot_last_script_backup`、`restore_last_script_backup_pointer`、`discard_orphan_script_backup` 三个辅助函数，`perform_script_update` 与 `perform_script_restore` 在改写指针前快照、在清理陷阱里于未提交时还原并清掉孤儿备份；「自动恢复也未能完成」路径置 `keep_orphan_backup=yes` 保留人工恢复所需备份；两处失败文案改为说明当前脚本未改动。新增验收 `test_replacement_failure_keeps_previous_restore_point`，改动前指针从 1.0.0 备份被顶成 1.1.0 备份、改动后保持不变且撤销仍能退回 1.0.0。未处理：`prepare_legacy_config_for_version`（`setup.sh:2748`）在撤销失败后留下的旧位置配置副本不回滚——该函数仅在退回 `CONFIG_RELOCATION_VERSION` 以下版本时才生效，残留是与当前配置逐字节相同的 0600 root 副本，已单独立项为 BUG-015 |
| BUG-002 | P1 | 完成 | 本文件（2026-08-14 审查） | 构建注入的 `materialize_roles` 无条件覆盖外层 EXIT 陷阱 | `tools/build-single-file.sh:94`（注入点 `:269-282`）无条件 `trap ... EXIT`，导致 `guided_reconfigure`（`setup.sh:2106`）的配置事务回滚陷阱和 `cleanup_cn_entry_operation`（`setup.sh:800-810`）被替换：重配置中途失败不还原旧配置，安装/回滚结束后 SSH 控制会话不主动关闭。已修：新增 `install_runtime_exit_trap` 与 `runtime_chain_exit_cleanup`，无既有 EXIT 陷阱时行为不变，已有陷阱时改为串接（先 `cleanup_runtime`，再把 `$?` 还原成原退出码后调用原处理函数）；处理函数名不是可安全串接的标识符时保留调用方陷阱并提示临时目录位置。新增验收 `test_materialize_roles_preserves_caller_exit_trap`：构建单文件后在带 EXIT 陷阱的子 shell 内调用 `materialize_roles`，断言退出码原样传出、外层清理执行、临时目录已清理；改动前该用例报「覆盖了调用方的 EXIT 陷阱」 |
| BUG-003 | P1 | 完成 | 本文件（2026-08-14 审查） | 无终端输入时 `prompt_required_value` 无限空转并占死操作锁 | 已修：`prompt_value` 与 `prompt_required_value` 在 `read` 读到 EOF 且未取得内容时 `die`，末行不带换行的有效输入仍按正常输入处理；新增验收 `test_prompt_eof_fails_instead_of_looping` 复现真实调用点（命令替换处于 `\|\|` 列表内、errexit 被抑制），改动前超时未退出、改动后失败退出。未采纳原计划中给 `configure` 加 `[[ -t 0 ]]` 守卫：`tests/install-entry-acceptance.sh:277` 用 heredoc 驱动 `configure yes` 是合法的非交互用法，加守卫会误伤 |
| QA-001 | P1 | 完成 | 本文件（2026-08-14 审查） | 「安全修复须先经用户确认」与两端 `health()` 返回码零运行时覆盖 | `tests/health-acceptance.sh:314` 只比较源码文本行号先后，`:262` 只跑过全绿路径。变异验证：把 `setup.sh:1605` 的拒绝分支改成空操作后，8 套 136 项验收仍全绿。仿 `tests/health-acceptance.sh:218-308` 的夹具手法补运行时用例，断言 `allow_repair=no`／非 TTY／回答 n／EOF 四种情况下 `EXIT_CMD_REPAIR` 调用次数为 0，并覆盖 `health()` 的 0／2／1 三种退出码 |
| BUG-012 | P1 | 完成 | 本文件（2026-08-15 使用反馈） | v2.5.20 回归：继承来的 EXIT 陷阱被误当成本层陷阱串接 | 用户升级到 v2.5.20 后健康检查末尾出现「警告：拒绝清理异常 SSH 控制目录：/run/po0-cn-ssh.*」。根因是 bash 在 `( )` 子 shell 里用 `trap -p` 会显示父 shell 的陷阱、而该陷阱在子 shell 内并不生效；`health_check` 与 `diagnostic_report` 都是子 shell 函数，串接后会在它们退出时提前执行 `cleanup_cn_entry_operation`——关闭 SSH 主连接、删除控制目录、提前释放操作锁。已修：`setup.sh` 的 11 处 EXIT 陷阱安装点先调用 `po0_claim_exit_trap` 声明归属（`${BASHPID:-$$}.${BASH_SUBSHELL}`），`install_runtime_exit_trap` 只在归属与当前层级一致时串接。新增验收 `test_materialize_roles_ignores_inherited_exit_trap`；bash 5 上实测修复前外层清理执行两次（level=1 与 level=0），修复后只在 level=0 执行一次 |
| BUG-014 | P1 | 完成 | 本文件（2026-08-15 使用反馈） | v2.5.21 回归：本地专有函数被写进发往国内入口的命令载荷 | 用户在 v2.5.21 上执行 Agent 扫描，对端报 `bash: line 8: po0_install_exit_trap: command not found`，扫描失败。根因是 BUG-013 修复时用正则批量把 `trap X EXIT` 换成 `po0_install_exit_trap X`，误改到 `setup.sh` 中通过 `ssh_cn_entry_tty` 发给对端执行的多行载荷——那段内容只是字符串，本地 `bash -n` 与八套验收都不会执行它。已修：该处还原为 `trap cleanup EXIT`；新增验收 `test_remote_payloads_use_no_local_only_functions`，静态提取全部多行远端载荷并核对不引用任何只存在于本地的函数（载荷内部自定义的函数除外），把本地辅助函数写回载荷后立即失败。教训：对同时包含本地代码与远端载荷的文件做批量替换时，必须逐处确认作用域 |
| BUG-015 | P3 | 完成 | 本文件（2026-08-14 审查的遗留项，2026-08-15 补立项） | 撤销更新失败后旧位置的连接配置副本不回滚 | `perform_script_restore` 在改写恢复点指针前调用 `prepare_legacy_config_for_version`，该函数会把当前连接配置复制到旧位置 `/root/hosts.conf`；随后任一步失败时，清理陷阱只还原指针、删除孤儿备份，不撤销这份副本。成功路径由 `finalize_legacy_config_for_version` 收尾，失败路径没有对应动作。触发面窄：只有退回到 `CONFIG_RELOCATION_VERSION` 以下的版本才会执行，残留是与当前配置逐字节相同的 0600 root 文件，不含额外信息、也不会被任何流程读取为当前配置。已修：`prepare_legacy_config_for_version`（`setup.sh:2749`）用 `LEGACY_CONFIG_COPY_CREATED` 声明这份副本是不是本次新建的，`perform_script_restore` 的清理陷阱（`setup.sh:3205`）在未提交时删除它，两个提交点（`setup.sh:3276`、`setup.sh:3293`）清标志。撤销之前就存在的副本不是本次新建的，任何失败路径都不删。新增验收 `test_restore_failure_removes_only_its_own_legacy_config_copy`，覆盖「本次新建后失败」与「旧位置原本就有副本」两种情形；同时更新 `test_restore_pre_24_script_and_config_together` 里针对旧行为的断言。变异验证：去掉清理动作、把清理改成无条件执行，两种改法都被抓出。该标志必须是全局量——见 QA-006 |
| QA-006 | P3 | 完成 | 本文件（2026-08-17 实现 BUG-015 时发现） | 事务清理陷阱依赖函数局部量，bash 3.2 下命令替换调用时读不到 | `perform_script_update` 与 `perform_script_restore` 都是子 shell 函数，其 EXIT 陷阱原先读所在函数的局部量（更新侧 `backup`、`pointer_snapshot`、`restore_pointer`、`keep_orphan_backup`，撤销侧 `current_backup`、`replacement`、`pointer_snapshot`、`restore_pointer`）。实测：bash 5.3 下无论直接调用还是 `$(...)` 命令替换调用，陷阱都读得到；bash 3.2 下只有直接调用读得到，命令替换调用时函数作用域已经弹出，陷阱读到未设置，于是指针还原与孤儿备份清理被静默跳过——正是 BUG-001 要防的那种恢复点丢失。生产环境是 Debian（bash 5），因此没有已知的用户可见影响。已修：两个函数的事务状态统一改为在函数内初始化的 `UPDATE_TRANSACTION_*`／`RESTORE_TRANSACTION_*` 全局量（沿用既有的 `UPDATE_TRANSACTION_CANDIDATE` 写法），子 shell 退出即丢弃，不会漏到调用方；`rc` 由陷阱自己赋值，从局部声明中移除。新增两条验收：`test_transaction_cleanup_runs_inside_command_substitution` 在命令替换里跑完更新与撤销的替换失败路径（bash 3.2 上有判别力，bash 5 上是回归保护），`test_transaction_cleanups_read_no_caller_locals` 从源码提取两个函数的局部量清单并断言陷阱一个都不读（任何 bash 版本都有判别力）。变异验证：换回改动前的 `setup.sh`，前者报「没有还原上一版恢复点」（指针被本次备份顶掉），后者报「清理陷阱读了局部量 backup」 |
| BUG-013 | P3 | 完成 | 本文件（2026-08-15 使用反馈） | 子 shell 在释放内置组件之后再装 EXIT 陷阱会覆盖组件清理，`/tmp/po0-unlock.*` 残留 | `scan_agent_services`（`setup.sh`）先调用 `preflight`→`materialize_roles` 安装 `runtime_exit_cleanup`，随后 `scan_agent_services_inner` 的 `trap cleanup_scan_remote EXIT` 把它覆盖掉，于是该子 shell 退出时不再清理内置组件临时目录。这是 v2.5.20 之前就存在的行为，不是 BUG-012 的回归，但会在 `/tmp` 留下 `po0-unlock.XXXXXXXX` 目录（0700 root，内含两个组件副本）。已修：新增统一入口 `po0_install_exit_trap`，11 处安装点全部改用它；安装前若发现本层当前的 EXIT 陷阱正是本层装的 `runtime_exit_cleanup`，先调用 `cleanup_runtime` 释放内置组件再覆盖。`install_runtime_exit_trap` 同步记录归属，使这一判断只对本层生效、不会误清父层的目录。新增验收 `test_late_exit_trap_releases_runtime_components`；把释放动作去掉后立即失败，bash 3.2 与 5.3 下均通过 |
| BUG-004 | P2 | 完成 | 本文件（2026-08-14 审查） | 托管 drop-in 被外部删除后完整回滚永久卡死且产品内无自救入口 | `src/cn-entry-role/95-role-status-rollback.sh.inc:206-212` 遍历托管清单调用 `disable-service`，任一失败即 `die`；`70-helper-command-dispatch.sh.inc:405-406` 要求 `managed_dropin_owned`，而 `50-helper-service-transactions.sh.inc:164` 第一句就是 `[[ -f ${file} ]] || return 1`。清单条目只能由 `disable-service` 成功路径移除，扫描菜单把该单元标为 `托管记录异常`（`90-role-agent-management.sh.inc:283`）且 `managed=no`，被 `:322-323` 拒绝处理。`health` 会报「国外出口配置缺失或已被修改」但不提供修复。已修：`disable-service` 区分「文件不存在」与「文件被人工修改」，前者按已移除处理并继续注销托管记录，后者仍然拒绝；`disable_transaction_cleanup` 新增 `TX_DROPIN_WAS_MISSING`，回滚时不重建原本就不存在的文件。新增验收 `test_disable_service_handles_externally_removed_dropin`，提取真实 `disable-service` 分支（仅把硬编码的 systemd 根目录换成夹具目录）实跑三种情形；改动前「文件缺失」一例报 rc=1 并保留托管记录 |
| SEC-002 | P2 | 完成 | 本文件（2026-08-14 审查） | `repair` 的隧道单元归属校验跳过全部主机密钥参数 | `overseas-exit-role.sh:756-757` 用两条 `grep -Fq` 只锚定 ExecStart 首尾子串，`-i`、`UserKnownHostsFile`、`StrictHostKeyChecking=yes`、`ExitOnForwardFailure` 均不在校验范围，且与同文件 `:747` 对代理单元的整行精确匹配不一致。被人工加入 `StrictHostKeyChecking=no` 的隧道仍会被判为「属于本助手」并 `systemctl enable --now`。已修：新增 `tunnel_exec_start_line`，写入与校验共用同一份模板（重构前后生成结果逐字节一致）；`repair` 改为要求单元中只有一条 `ExecStart` 且与重建行精确相等，拒绝提示同时告知可重新执行连接更新。新增验收 `test_repair_verifies_tunnel_host_key_options`，覆盖未改动、插入 `StrictHostKeyChecking=no`、追加第二条 `ExecStart` 三种情形；把校验换回旧的两条子串 grep 后，第二种情形立即失守。未处理：`health` 对隧道单元只检查权限、内容被改仍报「文件完整」，见 QA-005 |
| OPS-002 | P2 | 完成 | 本文件（2026-08-14 审查） | 管理 SSH 通道无保活，修改型远程调用远端不设上限 | `setup.sh:662-677` 只有 `ConnectTimeout=10`，缺 `ServerAliveInterval`（反向隧道在 `overseas-exit-role.sh:487` 是配了的），链路黑洞时最长等 TCP keepalive 约 2 小时且全程占锁；`setup.sh:671` 的修改型调用只在本地包 `timeout`，远端无上限，超时重试可能撞上孤儿进程。已修：控制主连接与 `authorize_admin_key_once` 加入 `ServerAliveInterval=15`、`ServerAliveCountMax=4`（多路复用会话共用主连接传输，因此覆盖全部 `-S` 调用）；`ssh_cn_entry_component` 把上限压到远端执行，本地上限改为远端上限加 15 秒兜底余量。新增 `shell_single_quote` 以 POSIX 语法包装远端命令，不依赖远端登录 shell 是 bash。新增验收 `test_management_ssh_uses_keepalive`，并扩充既有超时用例：断言本地 45s／135s 兜底、远端 30s／120s 上限，以及引号包装能被 `/bin/sh` 原样还原（含单双引号与空格） |
| QA-005 | P3 | 完成 | 本文件（2026-08-14 审查） | `health` 对隧道与代理单元只检查权限不检查内容 | `overseas-exit-role.sh:644-651` 只用 `health_regular_root_file` 判权限，内容被人工改过（例如插入 `StrictHostKeyChecking=no`）仍报「文件完整」。SEC-002 已让 `repair` 精确校验，但 `health` 的结论仍会误导。已修：复用 `tunnel_exec_start_line` 重建 `ExecStart` 并 `grep -Fqx` 精确比对，同时要求只有一条 `ExecStart`。迁移问题按提示措辞解决——不一致时明确指向「重新执行连接更新」，不再引导去点会被拒绝的安全修复；连接记录缺失时单列一种异常。新增验收 `test_health_verifies_tunnel_exec_start`，覆盖未改动、插入 `StrictHostKeyChecking=no`、连接记录缺失三种情形；回退内容校验后立即失败。两个既有健康检查夹具同步补齐了隧道与代理单元的真实内容 |
| QA-004 | P2 | 完成 | 本文件（2026-08-14 审查） | 两端 `health()` 的返回码没有运行时覆盖 | `tests/health-acceptance.sh:262` 起只跑过全绿路径。国外出口 `health()`（`overseas-exit-role.sh:594`）只有 0／1 两种返回码，国内入口 `health()`（`src/cn-entry-role/95-role-status-rollback.sh.inc:130`）还有 warnings 对应的 2。已修：新增 `test_health_exit_codes_are_runtime_covered`，实跑两端 `health()`——国内入口覆盖全绿 0、托管 Agent 配置完整但未运行的提醒 2、安装记录损坏的异常 1；国外出口覆盖全绿 0 与异常 1（该端没有提醒态）。提醒态需要真实的 drop-in 文件，因此夹具只把硬编码的 systemd 根目录换成临时目录，其余逻辑原样执行。变异验证：把提醒并入正常、把国外出口异常改成返回 0，两种改法都被抓出。同时给该套件补上缺失的 `assert_eq` 辅助函数 |
| QA-002 | P2 | 完成 | 本文件（2026-08-14 审查） | 三处运行时行为只靠源码字符串 grep 验证 | `tests/komari-acceptance.sh:631`、`tests/cf-probe-acceptance.sh:592`、`tests/komari-acceptance.sh:452` 违反 `AGENTS.md:15`。判别力可量化：`TX_KOMARI_IDENTITY_CREATED` 在产物中出现 6 次，删掉两处真实赋值仍剩 4 次、断言照样通过；CI 两个作业都装了 jq，`10-helper-identity.sh.inc:166-186` 的 python3 分支与「两者都没有」分支从未执行。已修：新增 `test_enable_rollback_removes_identity_guard`，实跑 `enable_transaction_cleanup` 并断言身份守卫、cf-probe 动态配置守卫与托管清单三项撤销，同时覆盖「未创建过守卫不得误删」和「事务已提交不撤销」；cf-probe 事务回滚的运行时覆盖由 BUG-005 的 `test_enable_rollback_compensates_before_dropin_flag` 承担。新增 `test_identity_guard_parser_selection`，用受控 PATH（只软链守卫真正需要的命令）覆盖 jq-only、python3-only 与两者都没有三种情形，断言缺少解析器时保守保留身份、不发起面板验证并记录原因。变异验证：把保守分支改成直接隔离身份、删掉身份守卫回滚标志的赋值，两种改法都被抓出。本机缺少某个解析器时该分支会打印说明并跳过，不做静默通过 |
| BUG-005 | P2 | 完成 | 本文件（2026-08-14 审查） | `enable-service` 的 cf-probe 回滚被错误嵌在 drop-in 判断内 | `src/cn-entry-role/50-helper-service-transactions.sh.inc:220-235` 把 go-pending 回滚与 `TX_COMPAT_CREATED` 清理放在 `TX_DROPIN_MAY_EXIST == yes` 块内，而改写动作在 `70-helper-command-dispatch.sh.inc:129` 就已执行、标志到 `:155` 才置位。窗口内失败会跳过整块补偿，留下未进托管清单的测速目标改动与 go-pending 残留。已修：go-pending 回滚与 `TX_COMPAT_CREATED`／`TX_COMPAT_CURL_CREATED` 清理提到顶层，只保留 rm drop-in、rmdir、daemon-reload、重启在原判断内，与 `refresh_transaction_cleanup` 同构。新增验收 `test_enable_rollback_compensates_before_dropin_flag`，直接以 `TX_DROPIN_MAY_EXIST=no` 调用清理函数并断言补偿被执行、事务已提交时不执行；改动前报「没有恢复 cf-probe 本轮测速目标」。同时把 `tests/cf-probe-acceptance.sh` 移入 `tools/check-shell.sh` 的动态夹具分组（该套件现在也动态抽取函数片段，夹具变量无法被静态追踪） |
| SEC-003 | P3 | 完成 | 本文件（2026-08-14 审查） | cf-probe 兼容包装脚本的归属校验漏掉权限位 | `src/cn-entry-role/20-helper-cf-probe.sh.inc:657`（兼容目录连属主都不查）、`:666-667`、`:678-679` 只校验属主与硬链接数，与同文件 `:202-204`、`:430-432`、`:442-443` 的同族守卫不一致；这两个包装脚本会被注入服务 PATH 最前面。已修：新增 `cf_probe_compat_mode_safe`（只拒绝 group/other 可写，兼容旧部署的更严格权限），兼容目录补属主校验，nc 与 curl 包装脚本补权限校验。新增验收 `test_cf_probe_compat_ownership_checks_mode`，覆盖正常、包装脚本人人可写、目录人人可写、旧部署更严格权限四种情形；去掉权限校验后立即失败 |
| BUG-006 | P3 | 完成 | 本文件（2026-08-14 审查） | 国内入口 `active_state()` 缺属主、权限与符号链接守卫 | `src/cn-entry-role/00-runtime.sh.inc:144-151` 只做 `-r` 与前缀判断且 `-d` 会跟随符号链接，同职责的 `overseas-exit-role.sh:148`、`95-role-status-rollback.sh.inc:36`、`setup.sh:1698` 都有完整校验。已修：两个函数都改为要求 ACTIVE 是 root 所有的 0600 普通文件且单一硬链接、状态目录是状态根的直接子目录且为 root 所有的 0700 真实目录。新增验收 `test_cn_entry_active_state_guards_paths`，覆盖正常、状态目录被换成符号链接、ACTIVE 权限异常、路径带 `../` 四种情形；恢复旧的宽松判断后立即失败 |
| BUG-007 | P3 | 完成 | 本文件（2026-08-14 审查） | `write_helper` 的 `/tmp` 临时文件没有清理兜底 | `src/cn-entry-role/10-helper-identity.sh.inc:4` 的 `mktemp` 未登记进任何 trap，脚本头是 `set -Eeuo pipefail`，任一步失败或收到信号都会留下 root 属主残留文件且无人上报；项目其他事务一律有 trap 兜底。改为子 shell 加 `trap 'rm -f -- "${tmp}"' EXIT INT TERM HUP`，并给 `mktemp` 补 `|| die` |
| BUG-008 | P3 | 完成 | 本文件（2026-08-14 审查） | `reconcile-cf-probe` 拿不到写锁时静默当成功退出 | `src/cn-entry-role/70-helper-command-dispatch.sh.inc:56` 是 `flock -n 9 || exit 0`。该分支由 `.path` 守卫触发，静默放弃后面板的新配置版本号已被接受、`.path` 不再触发，安全测速目标可能长期不恢复。已修：改为有界等待并在超时后非零退出、写明原因。新增验收 `test_reconcile_cf_probe_waits_for_lock`，以桩记录 `flock` 的真实调用参数并模拟等待失败，断言退出码非零、参数为 `-w 30`、不再出现 `-n`；改回非阻塞静默退出后立即失败 |
| BUG-009 | P3 | 完成 | 本文件（2026-08-14 审查） | 端口允许前导零导致重装后的指纹替换向导永不触发 | `setup.sh:140-142` 的 `valid_port` 放行 `022` 并原样写盘，而 ssh 与 ssh-keyscan 会归一成 22；`setup.sh:1032` 用字符串比较判默认端口，于是 `ssh-keygen -F` 查不到记录，`setup.sh:1108-1110` 直接 `return 1`，重装后的指纹替换向导永远不出现。已修：`validate_peer_config` 校验通过后统一 `$((10#...))` 归一化。新增验收 `test_ssh_port_is_normalized`，覆盖 `022` 归一为 22 且 `known_hosts` 键用裸地址、`02222` 归一为 2222 且键为 `[ip]:2222`、端口 0 仍被拒绝；回退归一化后立即失败 |
| BUG-010 | P3 | 完成 | 本文件（2026-08-14 审查） | 国外出口封存 ACTIVE 缺少国内入口同一步骤已有的防护 | `overseas-exit-role.sh:823` 是裸 `mv "${ACTIVE_FILE}" "${state}/ACTIVE.closed"`：无 `--`、无目标预存在检查、无符号链接检查、无 `|| die`，而等价步骤 `95-role-status-rollback.sh.inc:332-336` 是三重加固的。回滚重跑会静默覆盖上一份封存记录；`mv` 失败还会带着已删除的隧道单元继续打印「已回滚」。对齐国内入口写法并补「`ACTIVE.closed` 已存在时拒绝覆盖」验收 |
| BUG-011 | P3 | 完成 | 本文件（2026-08-14 审查） | Komari 展示 IP 菜单输错一个字符会把已成功的配置报成扫描失败 | `src/cn-entry-role/90-role-agent-management.sh.inc:161` 无效选项走 `die`，直接结束整个组件进程，而同文件 `:213` 处理同类输入用的是 `return 1`。该调用发生在代理已启用、ENABLE 已入日志、单元已进托管清单之后（`:338-348`），用户会看到与实际状态矛盾的「扫描未完成」，若据此执行完整回滚会白拆一套已生效配置。已修：无效选项改为提示并 `return 1`，调用点区分返回值并说明国外出口配置已经生效。新增验收 `test_report_ipv4_menu_invalid_choice_is_not_fatal`，断言无效输入返回非零、不触发 `die`、并说明未做修改；改回 `die` 后立即失败 |
| QA-003 | P3 | 完成 | 本文件（2026-08-14 审查） | ShellCheck 门禁漏掉发布产物且测试套件按文件名硬编码 | `tools/check-shell.sh:16-20` 的清单包含 `cn-entry-role.sh` 却漏了 `po0-unlock.sh`，而 `tools/build-single-file.sh:51-161` 注入的约 110 行运行时代码以 heredoc 形式存在、ShellCheck 只当数据（实测加入后当前零告警）；`:22-36` 又把 9 个测试脚本硬编码，而注册守卫与 CI 对同一批文件是通配发现的，新增套件不会自动进入静态检查。已修：生产组补入 `po0-unlock.sh`，工具与测试套件改为 glob 发现（新增套件默认进入动态夹具组，宁可多排除一个 SC2034 也不漏检）；新增 `--list` 子命令输出将检查的文件清单，并在 `tests/check-acceptance-registration.sh` 中核对该清单覆盖全部生产脚本、产物、工具与测试套件。加入 `po0-unlock.sh` 后门禁立刻抓到 BUG-002 注入代码里的一处 SC2064（trap 字符串立即展开），该处属于有意为之，已加注原因并定点豁免。用 glob 而非 `mapfile`：本脚本也要在开发机上运行，macOS 的 bash 3.2 没有该内建 |
| SEC-004 | P3 | 完成 | 本文件（2026-08-14 审查） | 归属校验只覆盖安装会话，菜单完整回滚无标识可用 | `setup.sh:3199-3200`、`:3210-3211` 发的是裸 `rollback-services`／`rollback-finalize`，国内入口对应分支（`95-role-status-rollback.sh.inc:352-378`）没有 `active_install_claim_matches` 闸门。经核实这不是简单漏用：`install_claim` 由 `setup.sh:1432` 现场生成、只在本次安装会话内使用，出口侧从不持久化，因此后续会话没有标识可传。可达前提是「更新连接配置」走 `authorize current allow-active`（`setup.sh:2113`）而 `allow-active` 把占用检查置空（`setup.sh:1191`），使一台出口机得以指向另一套已部署的入口。方案已与项目所有者商定（2026-08-15）：不做出口侧持久化标识（改动涉及两端、且历史部署无标识需额外兼容策略），改为堵住唯一的可达路径。已修：新增 `reconfigure_entry_policy`，`guided_reconfigure` 先记录当前入口地址，换到另一台入口时改用 `require-unclaimed`，同机重配仍用 `allow-active`（自己已有的 ACTIVE 不该拒绝自己）；只比较地址，同机换 SSH 端口不算换机。占用拒绝提示补上「先在那台上完成完整回滚」这条可行出路。新增验收 `test_reconfigure_refuses_claimed_entry`，既直接断言判定函数的三种输入，又把 `guided_reconfigure` 的硬编码状态路径换成夹具后实跑，断言传给 `authorize` 的策略；改回一律 `allow-active` 后立即失败。残留：裸回滚本身仍无归属校验，若今后出现新的可达路径需重新评估持久化标识方案 |
| DOC-001 | P3 | 完成 | 本文件（2026-08-14 审查） | `PROJECT.md` 称 CI 跑「九套验收测试」，实际八套 | `.github/workflows/ci-release.yml` 在注册检查之外执行 8 个套件，`tests/check-acceptance-registration.sh` 也输出「8 个套件、136 项」。改为「八套」或写成「`tests/*-acceptance.sh` 全部套件」以免今后再漂移 |
| REL-001 | — | 完成 | [PR #5](https://github.com/Cr0ce11/po0-unlock-assistant-public/pull/5) | 准备并发布 v2.5.18 | 版本号、生成物、标签与 Release 一致；本地验收清单、公开 CI 与两个 Release 附件摘要核对全部通过 |
| FIX-001 | — | 完成 | [PR #1](https://github.com/Cr0ce11/po0-unlock-assistant-public/pull/1)、[PR #2](https://github.com/Cr0ce11/po0-unlock-assistant-public/pull/2)、[PR #3](https://github.com/Cr0ce11/po0-unlock-assistant-public/pull/3)、[PR #4](https://github.com/Cr0ce11/po0-unlock-assistant-public/pull/4) | v2.5.18 的四项修复与门禁加固 | 同行 IPv4 脱敏、单文件构建语义门禁、ShellCheck 基线与变量作用域、远程组件与入口写锁超时保护均已合并并随 v2.5.18 发布，详见 `CHANGELOG.md` |

## 当前最值得继续的顺序

1. 先修 BUG-021 与 SEC-006：把 cf-probe forward 的启用、刷新、拆除和归属校验收敛成可恢复事务。
2. 然后处理 BUG-023、BUG-022：先给 `po0 check` 建立可预测的总体时限，再收紧旧 Shell Agent 的菜单能力。
3. 运行代码问题全部关闭后处理 DOC-002，压缩本文件，只保留开放事项与仍有效的风险决定。
4. 2026-08-21 起由 Codex 实现和提交；按所有者当前分工，Claude 负责独立复核 Codex 的工作成果。该分工是描述性记录，不构成 `AGENTS.md` 之外的合并门禁；规则继续只在 `AGENTS.md`，不新增代理专属规则文件。

## 2026-08-14 全项目审查

本轮由多代理并行审查九个维度（更新器、SSH 与主机密钥、文件系统守卫、事务与锁、Bash 正确性、国内入口模块、构建与 CI、测试质量、文档一致性），每条发现再由独立复核者尝试证伪：初步 36 条，证伪 12 条，确认 22 条，合并同源后形成上表的 BUG-001 至 BUG-011、SEC-002 至 SEC-004、OPS-002、QA-001 至 QA-003、DOC-001。

结论：没有任何一条是外部可利用的漏洞，也没有必须立即处理的项。绝大多数属于「同一份代码库里已有正确写法、某条路径漏了」的一致性缺口或回归防护缺失。审查全过程只读，未修改代码，未连接任何服务器。

任何需要登录服务器、修改 SSH、操作生产环境或改变公开仓库设置的事项必须独立执行，先记录回退方式并取得明确授权。
