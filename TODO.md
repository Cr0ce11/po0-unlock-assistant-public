# 项目待办

本文件只保存尚未完成的可执行事项，以及外部条件变化时必须重新评估的决定。状态变化通过 Pull Request 更新，不能只在聊天中标记；完成事实归入 [`CHANGELOG.md`](CHANGELOG.md)、[`PROJECT.md`](PROJECT.md) 或 [`docs/HANDOVER.md`](docs/HANDOVER.md)，不在这里长期堆积。

本仓库没有启用 GitHub Issue（项目所有者 2026-08-17 决定不启用），因此新事项应在这里登记编号，并在对应 Pull Request、Release 或仓库内的长期文档中留下正式记录。

## 当前开放事项

| 编号 | 优先级 | 状态 | 正式记录 | 事项 | 完成条件 |
|---|---|---|---|---|---|
| OPS-004 | P3 | 待确认 | [`PROJECT.md`](PROJECT.md) 的 cf-probe `auto` 模式长期结论 | 确认面板休眠且实时通道仍处于逻辑连接时，是否会出现上报曲线空档 | 由项目所有者在面板侧查看一段无人打开面板的休眠时段：若曲线无空档，将真机结果记入长期文档并关闭；若存在空档，新建缺陷评估补发或连接模式方案。探针侧日志无法单独完成这项确认 |
| QA-006 | P2 | 待办 | [PR #69](https://github.com/Cr0ce11/po0-unlock-assistant-public/pull/69) 的独立复核结论 | 为角色脚本跨进程调用建立系统性 dispatch 契约守卫 | 验收测试能自动核对角色经 `$0` 或 `${HELPER}` 发出的每个字面子命令，都存在于对应组件或 helper 的 dispatch；任意新增一个未登记子命令时必须失败。可安全执行的只读桥接还要通过真实角色入口运行夹具验证，不再只靠针对单个调用点的源码断言。守卫需覆盖 BUG-016、BUG-019 与 BUG-022 暴露的两侧独立正确、中间接线失配问题 |

## 条件变化时重新评估

以下事项不是当前待修缺陷，而是项目所有者已经接受、但不能永久遗忘的风险决定。满足“重新评估条件”时，应新建事项，不直接改写本表的历史决定。

| 编号 | 当前决定与现有缓解 | 重新评估条件 | 长期依据 |
|---|---|---|---|
| SEC-001 | 不注册改名前的旧 GitHub 账号名作为占位。v2.5.19 起更新器只接受当前账号下的公开仓库并明确拒绝旧地址；风险只落在仍使用 v2.5.18 及更早更新器的安装 | 发现仍在运行 v2.5.18 或更早版本的安装；项目开始由其他用户使用；旧账号名被他人注册或出现仿冒仓库 | [`CHANGELOG.md`](CHANGELOG.md) 的 v2.5.19 记录与更新验收 `test_updater_rejects_previous_owner_repository` |
| SEC-004 | 菜单完整回滚不增加出口侧持久归属令牌；更新连接改到另一台入口时按新装处理，拒绝接管已有 `ACTIVE` 的入口，已堵住当时唯一可达的误回滚路径 | 出现新的路径能让一台出口指向另一套已部署入口；开始支持多出口共享入口；现有占用检查无法覆盖新的安装或迁移方式 | [`CHANGELOG.md`](CHANGELOG.md) 的 v2.5.23 记录 |
| SEC-005 | 已从当前文件删除误写的生产面板域名，但不重写公开 Git 历史，以免破坏既有提交、标签和 Release 的对应关系；以后录入真机证据必须先移除主机名、IP 与端口 | 历史域名被用于定向探测；仓库成为长期对外样板而该关联不可接受；项目所有者不再接受当前暴露面。处置优先更换面板域名，而不是重写公开历史 | [`docs/HANDOVER.md`](docs/HANDOVER.md) 的“交回时的风险与回退”及 [`AGENTS.md`](AGENTS.md) 的安全红线 |
