# Po0 解锁助手

## 在哪里运行

每个 v2 Release 只提供一个正式脚本：

- `po0-unlock-v2.sh`：公开版，从公开 GitHub Release 匿名在线更新，不使用 GitHub Token，并保留上一版助手恢复等全部功能。

将它保存为国外出口 VPS 上的 `/root/po0-unlock.sh`，并在国外出口 VPS 内以 `root` 运行。

不要在 Mac 或国内入口上运行。国外出口和国内入口组件已经完整嵌入单文件，脚本只在需要时自动释放、校验和调用，不需要再上传或管理 `cn-entry-role.sh`、`overseas-exit-role.sh`。

## 作用

安装后，国内入口的以下管理操作会通过国外出口访问公网：

- APT 更新与安装
- curl、wget、Git
- 大多数读取标准代理环境变量的安装脚本
- 经明确启用的 systemd Agent 服务

现有 socat、Nfuse、SSH、DNAT 和其他关键中转连接不会被设置全局代理，也不会改变默认路由；ForwardX 只有在你从 Agent 扫描中明确选择并再次确认后，才会为其 systemd 服务单独启用国外出口。

如果选中的是 CF Probe，助手会多问一步「出站方式」。默认那种沿用至今，只给服务注入代理设置。如果面板上这台一直显示在线、但实时数据从来没上来过，就选第二种：它会额外起一个本地转发服务，专门给那条不读代理设置的实时通道用。多出来的文件和服务都归助手管，撤销时会一并拆掉；已经配置好的 CF Probe 也可以在管理菜单里改。

服务商在公网入口封禁的入站 80/443 等端口不会因此开放。需要对公网提供 Web 服务时，应另外使用国外出口反向代理。

## 地址识别方式

运行一键安装向导时只需输入：

1. 国内入口的连接 IPv4，优先填写服务商私网地址。
2. 国内入口当前 SSH 端口。

脚本会先查看国外出口到国内入口私网地址的实际路由，并自动识别国外出口私网源地址。如果国外出口本机没有可用的私网 IPv4，脚本会继续请求填写国内入口公网 IPv4，再按到该公网地址的实际路由自动识别并绑定国外出口源地址。

如果国外出口存在多个私网地址而脚本无法安全判断，它会停止并列出候选地址，不会擅自选择。

公网回退只改变 SSH 连接的两端地址，不会新开放端口。国内入口的 SSH 公网端口必须已经可从国外出口访问。

使用公网回退时，同一次安装、更新或检查会复用一条临时 SSH 控制连接，避免每个子步骤重新握手。只有不执行远程命令的首次建连会在失败后进行最多 3 次有限重试；上传和远程修改命令不会自动重复执行。

更新连接时，第 3 步会继续显示“组件同步、隧道稳定、代理与 Agent 刷新、最终状态检查”四个阶段及各自耗时。健康检查会显示国内入口检查准备、执行和总耗时；已安装组件与当前版本的属主、权限、硬链接数、SHA-256 和语法全部一致时会安全复用，否则仍临时上传当前组件。

同步国内入口正式组件时，助手会先拒绝既有目标为符号链接、异常硬链接、非 root 属主或非 `0700` 权限的情况，并保留原文件；新组件完成原子替换后还会复核普通文件属性、SHA-256 与 Shell 语法，复核失败即停止。

安装、更新连接、健康检查、Agent 扫描和完整回滚不能并行运行。助手会使用 `/run/po0-unlock/operation.lock` 做非阻塞互斥；如果另一条 `po0` 命令正在执行，当前命令会立即提示稍后重试，不会连接国内入口或修改服务。

正式国内入口组件调用会按工作量在 15 至 300 秒内停止无界等待。只读调用超时会明确说明本次调用没有修改入口；安装、刷新或回滚阶段超时会提示可能已经部分修改，并引导执行完整回滚。入口侧所有配置写锁最多等待 30 秒，超时后不会绕过锁继续；若完整回滚因此停止，请等待占用结束后重新运行完整回滚。

功能 4 读取 systemd 服务列表和服务元数据时均有超时上限，远端扫描组件整体最多运行约 60 秒；交互选择期间保持终端前台，输入 `0` 或确认不会被超时包装吞掉。超时或缺少 `timeout` 时会显示失败原因并安全结束，不会因信息读取卡住而继续执行 Agent 配置。

隧道更新完成后，助手会把国内入口地址、SSH 端口、国外出口源地址和隧道生效时间一起提交。任一状态文件替换失败会恢复旧记录；如果进程在提交期间中断，`.tunnel-state-commit.*` 事务备份会保留在当前安装状态目录，供后续排查。

国外出口的安装标记、隧道配置、主机密钥、代理配置和回滚目标会检查 root 属主、预期权限、普通文件类型和单一硬链接。发现符号链接、悬空链接、异常硬链接或异常目录时，健康检查、连接重配置和回滚会停止并保留现场，不会跟随外部路径。新建隧道密钥固定为私钥 `0600`、公钥 `0644`；完整回滚同时兼容旧版严格 umask 留下的 root 专用 `0600` 公钥，但不会接受更宽松权限。

## 把脚本放到国外出口 VPS

首次安装只需上传这一个脚本，并保存为：

```text
/root/po0-unlock.sh
```

然后 SSH 登录国外出口 VPS，运行：

```bash
bash /root/po0-unlock.sh
```

不要求预先执行 `chmod`，也不要求把说明文档或开发源码上传服务器。

第一次仍使用上面的 `/root/po0-unlock.sh` 进入菜单并完成一键安装。部署成功后，助手会自动：

- 把正式脚本安装到 `/usr/local/sbin/po0-unlock`；
- 创建 `po0` 快捷命令；
- 把连接配置保存在 `/etc/po0-unlock/hosts.conf`。

以后直接输入 `po0` 即可打开菜单。此时 `/root/po0-unlock.sh` 已经完成引导任务，可以删除；`/root` 下也不需要长期保留 `hosts.conf`。删除引导脚本不会影响菜单、代理服务或公开版的检查更新与恢复。

## 公开版：匿名在线更新

公开版通过 HTTPS 查询公开 GitHub Release，不要求、读取或保存 GitHub Token。运行 `po0`，选择 `6) 脚本更新与恢复`，再选择“检查并更新脚本”即可。

v2 不支持从 v1 原地升级。部署 v2 前，必须使用最后一个 v1 正式版执行完整回滚，再在已经清理的两台服务器上进行全新安装。v1 与 v2 使用不同的 Release 资产名，因此旧版更新器不会自动覆盖为 v2。

### 从旧私有版或旧分享版手动接管

旧私有版的更新器只认原私有仓库，旧分享版没有在线更新入口，因此迁移到单一公开版必须手动进行。

1. 从新公开仓库下载高于已安装版本的 `po0-unlock-v2.sh` 和 `po0-unlock-v2.sh.sha256`。旧 v2.5.16 迁移时应使用 v2.5.17 或更高版本。
2. 把两个文件上传到国外出口 VPS 的 `/root/`。
3. 在国外出口以 root 执行：

```bash
cd /root
sha256sum -c po0-unlock-v2.sh.sha256
bash /root/po0-unlock-v2.sh
```

只有摘要检查显示 `OK` 才继续。候选脚本会显示已安装版本和将安装的更高公开版；确认后只原子替换 `/usr/local/sbin/po0-unlock`，不修改代理、隧道、Agent 或连接配置。替换或最终自检失败时会恢复旧脚本。

4. 接管成功后复核已安装文件：

```bash
/usr/local/sbin/po0-unlock self-test
```

必须同时看到预期版本号、`Po0 单文件版本类型=公开版` 和 `SELF_TEST=PASS`。

5. 如果原来使用旧私有版，确认无误后删除 VPS 上的旧 Token 文件：

```bash
rm -f -- /var/lib/po0-unlock/updater/github-token
test ! -e /var/lib/po0-unlock/updater/github-token && echo '旧 GitHub Token 文件已删除'
```

最后到 GitHub 的 Personal access tokens 页面撤销该 Token。只删除 VPS 文件不等于撤销 GitHub 端凭据，两步都要完成。公开版不会读取、上传或自动删除这个历史文件。

## 首次安装：只运行一个命令

第一次在国外出口 VPS 内运行：

```bash
bash /root/po0-unlock.sh
```

进入中文菜单后选择：

```text
1) 一键安装（填写信息 + SSH 授权 + 自动安装）
```

接下来脚本会在同一次运行中连续完成：

1. 由你亲自填写国内入口连接 IP，优先填写私网；首次安装不允许留空。
2. 由你亲自填写国内入口 SSH 端口。
3. 自动识别并显示国外出口源 IP；私网路径不可用时继续请求国内入口公网 IP。
4. 显示完整执行摘要，等待一次最终确认。
5. 创建国外出口到国内入口的专用 SSH 密钥；如有需要，提示输入国内入口当前密码。
6. 自动安装、建立隧道并验证出口。
7. 扫描国内入口现有的监控/面板 Agent，由你按编号选择需要国外出口的服务。

不再需要分别运行 `configure`、`authorize` 和 `install`。

SSH 密码只由 SSH 程序在当前终端读取，不会写入脚本、配置或日志。`/etc/po0-unlock/hosts.conf` 只保存国内入口连接地址和 SSH 端口，配置目录和文件均仅 root 可以读取。

脚本将自动：

1. 验证国外出口源地址以及到国内入口 SSH 的可达性。
2. 在国外出口安装仅监听 `127.0.0.1` 的 Tinyproxy。
3. 创建独立 SSH 密钥和国内入口受限隧道账户。
4. 强制使用自动识别的国外出口源地址连接国内入口 SSH。
5. 在国内入口创建 `127.0.0.1:13128` HTTP 代理和 `127.0.0.1:19080` SOCKS5 代理。
6. 配置国内入口的 APT 和登录 shell。
7. 验证 HTTP、HTTPS 和 `apt-get update`。

安装完成后重新登录国内入口。以后在国外出口管理本方案时，统一运行：

```bash
po0
```

“完整回滚”只撤销代理、隧道和 Agent 配置，不删除正式助手或 `po0` 命令，方便以后重新安装。

## 密码、端口或 IP 变化时

### 只有国内入口 SSH 密码变化

如果国内入口没有重装、专用公钥仍在服务器上，无需操作。脚本使用公钥连接，不依赖已经变化的密码。

如果专用公钥被删除，在国外出口运行：

```bash
po0
```

选择 `2) 更新连接配置`，按提示重新填写国内入口连接 IP，随后输入国内入口当前密码即可。密码仍不会保存。

### 国内入口 SSH 端口或连接 IP 变化

在国外出口运行：

```bash
po0
```

选择 `2) 更新连接配置`，亲自填写新的国内入口连接 IP 和 SSH 端口。先尝试私网；如果国外出口没有可用私网源地址，再按新提示填写国内入口公网 IP。后续授权、指纹校验、隧道更新和出口验证会在同一次运行中自动完成。

脚本会重新识别国外出口源地址、校验国内入口 SSH 主机指纹并原地更新隧道，不重装 Tinyproxy，也不修改默认路由。

更新连接配置采用事务保护：确认前旧配置保持不变；授权失败或新反向隧道尚未稳定时，会自动恢复旧的 `hosts.conf`。新隧道确认稳定后，新配置会立即提交，后续 Agent 刷新或最终状态检查失败也会保留新配置，避免运行中的隧道与磁盘地址错位；此时可按提示修复 Agent 后重试检查。若旧配置自动恢复失败，事务备份会保留并显示准确路径。

首次授权或连接地址变化时，SSH 会先显示国内入口主机指纹。请将它与服务商控制台或你保存的可信记录核对后再确认；脚本会把已确认的记录保存到国外出口的 `/root/.ssh/po0-unlock-admin.known_hosts`，后续连接只接受这份记录。没有可信记录、记录权限异常或指纹不匹配时，不会发送密码，也不会执行国内入口命令。使用 `--yes` 进行首次连接时不能跳过这一步。

如果国外出口由旧版升级而来，旧版在严格 umask 下生成的 root 专用 `0600` 管理公钥可以继续安全复用；新生成的公钥仍为 `0644`。公私钥匹配只看算法与密钥数据，不会被不同 OpenSSH 版本附带的无关公钥注释影响。脚本不会为了兼容而放宽既有权限，普通用户可写、符号链接、额外硬链接或属主异常仍会停止流程。

同一台正在使用的国内入口不能同时作为两套 Po0 国外出口部署的测试目标。首次安装会在添加新管理密钥前和上传国内入口组件前分别进行只读占用检查；发现国内入口已有 `ACTIVE` 状态时会立即停止，不会修改现有 Agent、隧道或代理配置。即使两台出口恰好同时开始安装，失败清理也必须匹配各自的随机事务标识，不能回滚另一套部署。要验证完整安装、升级和回滚流程，请使用独立的国内入口测试机。

隧道建立后，国外出口还会通过专用管理连接确认国内入口的两个回环监听已经连续稳定；这一步同样只读取隧道专用 `known_hosts`，不会使用系统全局主机密钥记录。

### 国外出口 SSH 密码或公网端口变化

无需修改本方案。因为脚本已经在国外出口本机运行，不需要通过国外出口公网 SSH 连接自己。

### 国外出口私网 IP 变化

运行：

```bash
po0
```

选择 `2) 更新连接配置`。按提示重新填写国内入口连接 IP；脚本会自动识别国外出口源 IP 并更新隧道。如果不再具备共享私网路径，按后续提示改填国内入口公网 IP。

如果国内入口重装后 SSH 提示“主机密钥已变化”，运行 `po0` 并选择“更新连接配置”即可进入“服务器重装后的 SSH 指纹恢复”向导。向导会自动读取当前 `ed25519` 主机指纹并同时显示旧指纹、新指纹；新指纹只是网络扫描得到的候选值，请先通过服务商控制台确认确实发生了重装或更换，再输入精确的 `REPLACE` 才会替换记录。输入 `0`、其他内容、直接回车或遇到 EOF 都会取消并保留旧记录，`--yes` 也不能绕过这一步。替换前的 `/root/.ssh/po0-unlock-admin.known_hosts` 会生成 0600 权限备份，成功后会显示备份路径。正常情况下无需手工执行 `ssh-keygen -R`；只有向导无法读取当前指纹时，才应先排查 SSH 连通性后重试。

## 日常使用

以下命令在国内入口上使用：

```bash
apt update
apt install <软件包>
curl https://example.com
wget https://example.com/file
git clone https://example.com/repository.git
```

只让某条国内入口命令使用国外出口：

```bash
po0-cn-entry run <命令> [参数]
```

测试出口：

```bash
po0-cn-entry test
```

## 为国内入口 Agent 服务启用代理

安装完成后，Po0 解锁助手会自动扫描一次现有 Agent。以后又安装了新的 Agent，只需在国外出口运行：

```bash
po0
```

选择：

```text
4) 扫描 Agent 服务并配置国外出口
```

扫描器只列出运行中的疑似监控或转发面板 Agent，并明确排除 SSH、网络、防火墙、Docker、Nfuse、socat、sing-box、xray、realm、gost、nginx、Caddy 等关键中转服务。`forwardx-agent.service` 会作为“ForwardX 转发面板 Agent”列出，`flux_agent.service` 会作为“Flux 转发面板 Agent”列出；官方默认的 `nyanpass.service`，或描述仍为 `nyanpass` 的自定义服务名，会作为“NyanPass 转发面板 Agent”列出。脚本不会自动选择这些转发 Agent；是否为它们启用代理完全由你决定，确认页还会再次显示风险提示。服务显示 `active/running` 只说明进程正在运行，不代表已经成功连接面板。

功能 4 会复用已经安全校验且版本一致的国内入口组件，并显示连接准备、组件选择和服务信息读取进度。只查看列表或输入 `0` 取消时不会探测公网；确认修改某个服务后，才会并行验证 HTTP 与 SOCKS5 国外出口，两项都通过才进入配置。

它不会自动给所有服务加代理。每次只能按编号选择一个服务；如有多个 Agent，完成后再次扫描。选定后脚本会：

1. 只为所选服务创建独立 systemd 代理配置。
2. 重启一次并确认该服务恢复为 `active/running`。
3. 如果服务启动失败，立即移除刚添加的配置并尝试恢复原状态。
4. 将成功配置的服务记入回滚清单。

ForwardX 使用专用代理配置：只注入 HTTP/HTTPS 代理供面板控制连接和上报请求使用，不注入 `ALL_PROXY`/SOCKS5，避免把原始 TCP 转发数据面整体改道。它仍会在启用时重启一次，因此应在可接受短暂转发重建的时间执行。

### CF Probe 没有显示延迟

CF Probe 会用 TCP 测量延迟。当前官方 Go Agent 的默认测速端口是 80，而这台国内入口的 80、443、8080、8443、8000、1080 均被服务商双向封禁，因此 Agent 可能长期显示“没有延迟、100% 丢包”。

Po0 解锁助手会按已确认的 Agent 类型选择兼容方式：

1. 对当前已确认兼容的官方 Go v1.0.0 至 v1.0.6，直接使用 Agent 原生支持的 `主机:端口` 配置。只替换缺少端口或命中上述封禁端口的测速目标，并在写入前验证备用 TCP 53 目标可达；用户已有的其他安全端口目标保持不变。
2. 原配置中的密钥、面板地址和其他字段保持不变；原四项测速目标另存为 root 专用恢复记录。服务重启失败会撤销本轮修改，停用国外出口或完整回滚会恢复原目标。
3. 默认四项依次使用 `61.153.34.8:53`、`210.33.80.8:53`、`210.32.68.3:53` 与 `121.192.44.254:53`。它们由三所金华高校的官方域名公开为权威 DNS；这里不用可能改落入口附近的 Anycast。这些目标只在当前国内入口逐项连通验证成功后才会写入，任何一项失败都会保留原配置。
4. 官方 Go Agent 启动时可能立即接收面板动态配置。助手会在成功提示前自动收敛这次覆盖，并安装仅监视该配置文件的服务专用守卫；以后面板设置变化再次覆盖测速目标时，守卫会保留新的配置版本、密钥和其他字段，只恢复四项 Po0 测速目标并重启一次 Agent。停用国外出口或完整回滚时守卫会一并撤销；首次事务即使意外中断，下次运行也会先恢复再重试。
5. 对旧 Shell Agent，仍只在该服务内让 Agent 使用自身已有的 `ping` 回退，不修改系统全局网络和 `nc`。
6. 快照版、未经确认的 Go 修订版或不再具备旧 `ping` 回退的 Shell 版本会停止操作并保留原配置，不会猜测兼容。若正式 Go 版本已经受支持但运行参数或配置文件未通过属主、权限、路径等安全校验，助手会明确显示这一原因。

### CF Probe 显示成国外出口地区

CF Probe 经国外出口连接面板时，面板通常会把连接来源识别为国外出口。以下直连上报兼容只适用于已确认支持该调用方式的旧 Shell Agent；Po0 解锁助手会只针对它的面板上报请求，优先改用 Cloudflare 支持的备用 HTTPS 端口从国内入口直接发送：

1. 不修改 CF Probe 的配置文件，因此 Agent 自动更新不会覆盖这项处理。
2. 延迟探测从国内入口直接发出；Agent 更新和其他联网请求仍使用原有国外出口。
3. 直连成功时，面板会根据国内入口自身的公网来源显示真实地区。
4. 如果备用端口不可用，会自动退回国外出口上报，优先保证节点不在面板掉线。
5. 取消该服务的国外出口或执行完整回滚时，相关兼容文件会一并清理。

当前官方 Go Agent 使用自身网络实现，不会调用旧 Shell Agent 的 `curl` 包装，因此它的上报仍经国外出口，面板来源地区也仍可能显示国外出口。当前兼容只保证延迟探测使用未封禁的国内直连端口，不宣称修复 Go Agent 的来源地区显示。

### Komari 面板没有显示延迟

Komari 的 TCP 延迟由 Agent 内部直接建立连接，不读取普通的 HTTP/SOCKS 代理设置。用本地 TCP 转发器处理会提前完成连接，只得到没有意义的 `0 ms`，因此 Po0 解锁助手不会代理 Komari 的延迟任务。

请在 Komari 面板把该服务器的延迟任务设置为 **ICMP**：

1. Komari 的控制连接和普通上报仍通过国外出口，保证 Agent 在线。
2. ICMP 延迟从国内入口直接探测，显示的是这台服务器自身的真实网络延迟。
3. 面板展示 IP 仍可由助手固定为国内入口公网 IPv4。
4. 旧版助手创建过 TCP 延迟转发时，再次从 Agent 扫描中选择该 Komari，脚本会验证所有权后移除专用服务、配置和防火墙规则。
5. 旧版可能安装过 `redsocks`。由于脚本无法确认它是否后来被其他程序使用，只会保持其系统服务关闭，不会擅自卸载软件包。

如果旧版专用文件被人工修改，助手会拒绝自动删除并提示检查，避免误删外部配置。

### Komari 自动发现身份失效后不再上线

Komari 使用 `--auto-discovery` 注册后，会在程序目录保存 `auto-discovery.json`。如果面板已经不再接受其中的客户端令牌，Agent 仍可能一直复用旧文件，而不是用新的自动发现令牌重新注册。

Po0 解锁助手会为确认启用了自动发现的 Komari 加入服务启动前检查：

1. 只读取运行中 Agent 的面板地址、程序目录和“已启用自动发现”这一事实；不会复制或保存自动发现密钥。
2. 启动前通过面板的只读任务接口检查缓存客户端令牌，令牌经标准输入交给 `curl`，不会出现在命令行参数中。
3. 只有 `auto-discovery.json` 本身损坏，或面板明确返回 Komari 的 `401 Unauthorized` JSON 时，才隔离旧身份，让 Agent 本次启动重新自动发现。
4. DNS、网络、TLS、国外出口代理异常，以及 `404`、`5xx` 或无法确认的响应，均保留原身份并继续启动 Agent。
5. 被隔离的旧文件不会直接删除，而是以 `0600` 权限保存在 `/root/komari-identity-backups/`，便于人工追溯。

这项检查只在 Komari 服务启动或重启时运行，不进行周期轮询。已经由旧版助手托管的 Komari，再次从 Agent 扫描中选择，或刷新 Po0 连接配置，即可增量补齐。停用该 Agent 的国外出口或执行完整回滚时会移除守卫，但已经隔离的身份备份会保留。

### 检查或撤销单个 Agent 的国外出口

功能 4 扫描到已经由 Po0 配置的 Agent 后，会提供三个选择：

1. “检查并更新配置”会重新验证国外出口，再安全刷新这个 Agent 的当前配置。
2. “撤销国外出口配置”只移除这个 Agent 的 Po0 代理和相关兼容文件，不卸载 Agent，也不影响其他服务。
3. “返回”不执行网络探测，也不修改配置。

撤销前会再次确认。撤销成功后 Agent 恢复直接联网；如果国内入口仍受服务商限制，它可能无法连接面板。服务重启或清理失败时，助手会恢复撤销前的配置并报告失败。

### 让 Komari 面板显示国内入口 IP

Komari 经国外出口联网后，自动检测通常会把节点 IPv4 显示为国外出口。Po0 解锁助手现在可以只修改 Komari 上报给面板的展示 IPv4，同时让它的面板连接继续走国外出口：

1. 在 Agent 扫描结果中选择已经配置的 `komari-agent.service`。
2. 选择“设置或更新国内入口公网 IPv4”。
3. 填写国内入口的**公网 IPv4**并确认。

这不会改变国内入口的默认路由，也不会让 Komari 绕过国外出口；它只使用 Komari 官方的 `AGENT_CUSTOM_IPV4` 配置覆盖面板中的 IPv4 字段。扫描结果会显示“展示IP=固定地址”或“展示IP=自动”。

如需恢复原行为，再次选择 Komari，然后选择“恢复自动检测”。完整回滚或单独取消 Komari 的国外出口时，这项自定义展示 IP 也会一并移除。

高级用户仍可在国内入口上手动使用：

```bash
po0-cn-entry enable-service <服务名.service>
po0-cn-entry disable-service example-agent.service
po0-cn-entry set-report-ip komari-agent.service <国内入口公网IPv4>
po0-cn-entry clear-report-ip komari-agent.service
```

ForwardX 必须从 Agent 扫描中明确选择并确认，不要手工编辑它的 systemd 代理配置。仍不要对 Nfuse、socat 或其他受保护中转服务启用代理。

## 中文主菜单

在国外出口 VPS 输入 `po0` 后会看到：

```text
1) 一键安装（填写信息 + SSH 授权 + 自动安装）
2) 更新连接配置（地址/端口变化时使用）
3) 健康检查与问题处理
4) 扫描 Agent 服务并配置国外出口
5) 完整回滚
6) 脚本更新与恢复
0) 退出
```

通常只需使用这个菜单，不需要记忆任何子命令。菜单 5 的完整业务回滚仍会要求输入 `ROLLBACK`，防止误操作；如果是误触，可以输入 `0` 立即返回主菜单，其他无效输入会留在当前界面重新提示。真正执行后，它会停止隧道、移除国内入口代理配置和受限账户、恢复国外出口 Tinyproxy 安装前状态。两端时间戳状态目录会保留，便于审计。

### 健康检查与问题处理

选择主菜单 `3)` 后，助手会先进行完全只读的检查，不会因为检查而重启服务或修改配置。报告使用“正常、提醒、异常”显示，并按基础状态、代理、隧道、连接路径和 Agent 分组。每项采用“检查项：说明”的单行格式，不依赖终端字体宽度进行空格对齐：

```text
【1/2 国外出口】

  出口代理
    [正常] 国外出口代理配置：文件完整
    [正常] 国外出口代理服务：正在运行

  小结：[正常] 国外出口全部检查通过
```

检查范围包括：

1. 国外出口的安装记录、代理服务、反向隧道、开机启动、监听端口和外部联网。
2. 两台服务器之间当前连接路径的路由与国内入口 SSH 端口。
3. 国内入口的受限隧道账户、代理端口、APT 和登录环境配置。
4. 已由助手托管的 Agent 是否仍存在、配置是否完整以及当前是否运行。
5. 是否存在已知旧版 Komari 延迟组件。

只有发现明确异常时才会询问是否安全修复。确认后，自动操作严格限制为：

- 恢复本项目自己的国外出口代理服务和反向隧道服务；
- 校验并更新国内入口管理组件，更新前自动备份旧文件。

自动修复不会修改连接地址、SSH 配置、系统路由、Agent 配置或第三方文件。国外出口两个核心服务会作为一个整体处理；任一服务修复失败，脚本会尝试把两者都恢复到修复前状态。

如果配置文件归属不明、关键内容被修改、地址失效或国内入口无法连接，助手只报告问题，不会冒险覆盖。高级排障仍可运行 `po0 raw-status` 查看原始状态。

如果检查发现问题而安全修复没有解决，助手会询问是否生成诊断报告。报告会再次进行只读检查，汇总两台服务器的系统版本、磁盘使用率、核心服务、托管 Agent 数量、配置完整性和国外出口近期错误日志。报告还会显示连接配置读取、两端概况、完整健康检查与错误日志采集耗时，以及本次操作实际发生的国内入口 SSH 初始建连次数；国外出口当前隧道配置的生效时间会以 UTC 紧凑格式显示，无法安全验证时显示“未知”。它不会重启服务、修改配置或自动上传文件。

报告保存在国外出口的 `/var/lib/po0-unlock/diagnostics/`，目录仅 root 可以进入，报告文件仅 root 可以读取。生成时会自动隐藏地址、域名、账号、端口、令牌、密码、密钥和指纹；发送给他人前仍建议快速查看一遍。需要跳过菜单直接生成时，可以运行 `po0 diagnose`。

### 无人值守巡检

`po0 check` 把上面这套健康检查的结论用适合程序读取的方式输出，供定时任务或手机上的助手通过 SSH 定期调用。它不需要终端，不会画菜单、不会等待输入，也不会提出修复询问；发现问题只报告，不动手。

```text
po0 check          # 人读：一句话结论加逐项明细
po0 check --json   # 机器读：单行 JSON
```

调用方主要看退出码：`0` 全部正常，`1` 有提醒项，`2` 有异常项，`3` 表示检查本身没跑成（例如未安装、权限不足或连不上国内入口）。前三档与菜单里的“正常、提醒、异常”完全一致，第四档用来区分“服务坏了”和“这次没查成”。

它不会修改配置、重启服务或建立恢复点。为了读取国内入口的状态，它仍会像菜单里的健康检查一样建立一次临时 SSH 连接；如果国内入口上的组件与脚本内置的版本不一致，会先上传一份临时只读组件，用完即删。它与安装、回滚等操作共用同一把互斥锁：正好赶上别的操作在跑时，它会立刻返回退出码 `3`，不会排队等待，也不会插进去。

用 `--json` 时，正常结论和出错结论都从标准输出返回合法 JSON，出错说明走标准错误，因此可以直接 `po0 check --json 2>/dev/null | jq .`。

JSON 输出包含总结论、检查时间、脚本版本和逐项结果，每项的说明沿用诊断报告的脱敏规则，不会出现地址、端口、账号、密码或指纹：

```json
{"overall":"ok","checked_at":"2026-08-17T07:29:04Z","version":"2.5.25","checks":[{"name":"安装记录","status":"ok","detail":"完整"}]}
```

进入“脚本更新与恢复”后会看到：

```text
1) 检查并更新脚本
2) 恢复上一版助手（不改服务）
0) 返回主菜单
```

## 脚本更新与恢复

选择菜单 `6) 脚本更新与恢复`，再选择 `1) 检查并更新脚本`。脚本会通过匿名 HTTPS 请求查询公开 Release，不会查找或提示输入 GitHub Token。

从交互菜单完成更新或恢复后，助手会自动切换到对应版本的新界面。通过 `po0 update` 或 `po0 restore-script` 直接执行时，操作成功后会正常结束，不会额外打开交互菜单，便于自动检查或维护任务调用。

更新器只读取最新的正式、非预发布 GitHub Release，并依次检查：

1. 返回内容确实来自预期且公开的仓库。
2. Release 标签使用严格的 `vX.Y.Z` 格式。
3. Release 标签与下载脚本声明的版本完全相同。
4. 下载资产的 SHA-256 与 GitHub Release 摘要一致。
5. 候选脚本通过 Shell 语法检查和内置 `self-test`。

全部检查通过后，界面会显示当前版本、最新版本和摘要，再等待你的确认。确认后才会备份当前 `po0-unlock.sh`、原子替换脚本并切换到新版本。更新脚本本身不会修改正在运行的代理、隧道或 Agent 服务。

网络不可用、仓库身份或公开属性不符、Release 为草稿或预发布、附件 digest 不匹配时，更新器会在备份和替换之前停止。

## 恢复上一版助手与完整回滚的区别

这两个操作用途完全不同：

- `6) 脚本更新与恢复 → 2) 恢复上一版助手（不改服务）`：只恢复在线更新器备份的上一份 `po0-unlock.sh`。它不会改变代理、SSH 隧道、Agent、APT 或现有连接，适合新脚本界面或逻辑出现问题时使用。
- `5) 完整回滚`：撤销国内入口的业务部署，会停止隧道、移除国内入口代理配置和受限账户，并恢复国外出口 Tinyproxy 原状态。

恢复上一版助手只有在至少成功更新过一次并留下备份后才能使用。恢复前会再次校验备份版本、SHA-256、语法和自检；成功后会自动切换到旧版脚本进程。菜单 5 不等于脚本降级。

正常脚本更新和恢复成功后，助手会自动整理旧脚本备份，最多保留 3 份；当前可用于“恢复上一版助手”的备份不会被清理。人工测试使用的候选文件和人工备份不在自动清理范围内。发现备份文件异常时，助手会保留现有文件并给出提醒，不会为了节省空间冒险删除。

高级用户可以直接运行：

```bash
po0 update
po0 restore-script
```

## 安全边界

- 不保存 SSH 密码。
- 不新增公网监听端口。
- 国外出口和国内入口代理都只监听回环地址。
- 国外出口到国内入口的连接强制绑定实际路由自动识别出的国外出口源地址。
- SSH 主机密钥指纹会在启动隧道前校验。
- 不修改两台机器的默认路由、MTU、sysctl 或现有转发配置。
- 不给所有 systemd 服务设置全局代理。
- 安装、重配置与回滚均不重启 VPS。
- 首次初始化会先验证状态目录、`ACTIVE`、隧道密钥、管理密钥和国内入口托管目标；发现符号链接、异常属主/权限、重复状态目录或残留隧道家目录时，会在写入前停止并保留现场。
- 公开版不要求、读取、创建或管理 GitHub 更新令牌；旧私有版遗留的 Token 只能在新版接管确认后手工删除并到 GitHub 端撤销。
