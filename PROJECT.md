# 项目状态

## 项目目标

Po0 解锁助手通过国外出口 VPS，为受公网访问限制的国内入口提供可回滚的管理出站能力，并可让指定的 systemd Agent 使用国外出口。它运行在生产 VPS 上，属于安全关键工具。

项目当前最重要的目标不是继续增加功能，而是保持生产环境上的运行安全与可回退，以真实使用中发现的问题驱动维护，并继续使用可重复、可审计、可回退的开发与发布流程。

## 正式来源与文档职责

- GitHub 仓库 [`Cr0ce11/po0-unlock-assistant-public`](https://github.com/Cr0ce11/po0-unlock-assistant-public) 的 `main` 是代码和文档的唯一正式来源。
- Pull Request 记录实现范围、风险、测试证据和恢复方式。
- GitHub Release 是可部署脚本的唯一正式来源；每个正式版只提供 `po0-unlock-v2.sh` 与 `po0-unlock-v2.sh.sha256` 两个附件。
- 服务器只用于部署和运行，不能作为代码或项目记录的唯一来源。

聊天可以用于讨论和确认，但长期有效的结论必须进入以上正式记录。

本仓库**没有启用 GitHub Issue**：项目所有者 2026-08-17 决定不启用（`TODO.md` 的 GOV-002）。需求、缺陷和优先级由 [`TODO.md`](TODO.md) 配合 Pull Request、Release 和仓库内长期文档承载。

v2.5.17 以前的历史不在本仓库内：原私有仓库与本地冷归档已在公开版接管和恢复能力验证完成后按项目所有者授权删除，不再是恢复来源；更早版本的结论以 [`CHANGELOG.md`](CHANGELOG.md) 中保留的产品变更摘要和公开 Release 为准。

每类长期信息只维护一个主要正式来源：

| 信息类型 | 主要正式来源 |
|---|---|
| 当前版本、环境、发布形态和维护执行方 | 本文件 `PROJECT.md` |
| 可执行事项、优先级和状态 | [`TODO.md`](TODO.md) |
| 执行规则、源码与产物边界、安全红线、本地验收清单 | [`AGENTS.md`](AGENTS.md) |
| 工作区级规则（GitHub 认证、文件布局与清理） | [`../AGENTS.md`](../AGENTS.md) |
| 每个版本相对上一正式版的变化 | [`CHANGELOG.md`](CHANGELOG.md) |
| 用户可见行为与操作步骤 | [`README.md`](README.md)、[`使用说明.md`](使用说明.md) |
| 维护交接基线与交回记录 | [`docs/HANDOVER.md`](docs/HANDOVER.md) |
| CI 与发布门禁的实际实现 | [`.github/workflows/ci-release.yml`](.github/workflows/ci-release.yml) |

其他文件可以提供摘要和链接，但不重复维护同一套操作步骤或门禁清单。

## 当前状态

| 项目 | 当前状态 |
|---|---|
| 最新正式版 | v2.5.30（2026-08-19） |
| 主开发分支 | `main` |
| 仓库形态 | 公开，按 GPL-3.0-only 发布（2026-08-17 起，版权署名 `Cr0ce11`）；Issue 经所有者决定不启用（2026-08-17），协作入口只有 Pull Request |
| 运行位置 | 只在国外出口 VPS 上以 root 运行；国内入口组件只能由国外出口主控释放、传输、校验和调用 |
| CI 覆盖环境 | Debian 12（bookworm）与 Debian 13（trixie）容器矩阵 |
| 提交前门禁 | [`AGENTS.md`](AGENTS.md) 的《本地验收清单》，逐项执行且必须全部通过 |
| CI 门禁 | `ci-release.yml`：固定版本 ShellCheck 基线、版本号唯一性与标签一致性、单文件确定性重建、验收注册检查、`tests/*-acceptance.sh` 全部套件，Debian 双版本重复执行 |
| 发布形态 | 推送 `v*` 标签触发；先建草稿并上传两个附件，把 GitHub 返回的 digest 与本地 SHA-256 逐一核对后才发布；同标签 Release 已存在时拒绝 |
| 真机验证 | 本地与 CI 只覆盖语法、构建确定性和离线验收。项目所有者已决定（2026-08-14）不安排专门的验证机，由其在日后实际使用中验证；针对 v2.5.18 的专门验证已于 2026-08-17 关闭，理由是被后续版本的真机验证取代，详见 `TODO.md` 的 OPS-001。已进入正式记录的真机结果：2026-08-15 由项目所有者在自有环境确认 v2.5.22 的 Agent 扫描恢复正常（该路径在 v2.5.21 上因 BUG-014 失败）；健康检查在 v2.5.20 上暴露的 BUG-012 也是同一渠道发现；2026-08-17 在所有者授权的生产出口机上完成 v2.5.24 只读入口的真机验证（`check` 与 `check --json` 均正常，已安装脚本前后未变、无残留）。2026-08-19 由项目所有者在生产国内入口上完成 v2.5.28 的真机验证：cf-probe 以 forward 出站方式纳管成功，实时通道 `WSS connected`，四项测速目标已改写为国内直连 TCP 53 节点；该轮同时由实机反馈暴露并修复了 BUG-016 与 BUG-017。当时同步记录的「HTTP 上报与实时通道并存」是切换那一刻的瞬时观测，不是稳态——该探针默认的 `auto` 模式在实时通道连上后就不再走 HTTP 上报，断开才回退，详见 `TODO.md` 的 OPS-004。2026-08-19 维护交接核对时又在同一台入口上发现 BUG-018（转发单元没有开机自启），修复随 v2.5.29 发布并在该机确认生效；同日在 v2.5.29 的发布后核对中发现 BUG-019（健康检查的转发模式判定跨作用域调用，自 v2.5.26 起从未生效），修复随 v2.5.30 发布。v2.5.30 的真机验证已完成：更新后 `po0 check` 中转发模式的 Agent 显示带「（转发模式）」的说明，未启用转发模式的 Agent 仍为通用措辞；并经项目所有者按次授权做了一次故障注入演练——停掉国内入口的转发单元后，`po0 check` 退出码 2、整体异常且只有该 Agent 变红，恢复后回到 24/24 正常、无残留。这是本仓库历史中第一次经授权在生产环境上执行改变状态的操作，范围限于停止并立即恢复一个由本助手托管的转发单元。真机验证只作为发布后的额外确认，不属于提交前门禁——《本地验收清单》必须能完全离线完成。改变状态的真实服务器操作仍需项目所有者按次授权具体服务器 |
| 生产环境状态 | 2026-08-17 起，项目所有者授权维护方对其一台生产国外出口机执行**只读**验证（范围、边界与禁止事项见 [`docs/HANDOVER.md`](docs/HANDOVER.md) 的《真机只读验证授权：2026-08-17》）。默认仍是只读；2026-08-19 之前本仓库历史中没有记录过修改生产服务器的操作。2026-08-19 首次按次授权执行了两项改变状态的操作，均由项目所有者逐次明确授权、范围限定且当场核对了结果：一是清理该出口机上 5 个早期压测遗留的失败临时单元（`systemctl reset-failed`，无文件残留，与本项目无关），二是在生产国内入口上做一次故障注入演练——停止并立即恢复由本助手托管的 cf-probe 转发单元，用于验证 v2.5.30 的健康检查确实会报出该故障。除此之外没有其他写操作；线上版本升级仍由所有者自行执行。服务器地址、端口与凭据一律不入本仓库 |
| 已知未解决缺陷 | 2026-08-21 Codex 接管审查登记 5 项运行问题：BUG-020、BUG-021 为 P1，SEC-006、BUG-023 为 P2，BUG-022 为 P3；另有文档治理项 DOC-002。具体影响、完成条件与处理顺序见 [`TODO.md`](TODO.md) |
| 维护执行方 | 2026-08-21 起交回 Codex，由 Codex 负责实现、测试、PR 与发布准备；按项目所有者当前分工，Claude 负责独立复核 Codex 的工作成果。该分工不构成 `AGENTS.md` 之外的合并门禁。交回基线与既往维护期记录见 [`docs/HANDOVER.md`](docs/HANDOVER.md) |

## 职责边界

- 项目所有者保留最终业务验收、生产环境上线、高风险操作授权，以及公开仓库设置的决定权（Issue 是否启用、许可证的授予与更换、仓库可见性与保护规则）。
- Codex 负责日常实现、测试、文档、分支、Pull Request 与发布准备；按项目所有者当前分工，Claude 独立审查 Codex 提交的差异、测试证据、风险与回退方式，复核结论可记录在对应 Pull Request。这里记录职责，不新增必须依赖特定工具的提交门禁；长期执行规则仍以 [`AGENTS.md`](AGENTS.md) 为唯一来源，不建立任何代理专属规则文件或目录。
- 可能造成服务中断、数据丢失、权限变更、凭据暴露、不可逆写入或影响生产环境的操作，必须在执行前准备好回退方式并取得项目所有者明确授权。

## 如何更新本文件

当最新正式版、仓库形态、CI 覆盖环境、发布形态、生产环境状态、维护执行方或职责发生变化时，必须在同一个 Pull Request 中更新本文件。具体任务和进度不要堆积在这里，记录到 [`TODO.md`](TODO.md)；版本变化记录到 [`CHANGELOG.md`](CHANGELOG.md)。
