# 项目状态

## 项目目标

Po0 解锁助手通过国外出口 VPS，为受公网访问限制的国内入口提供可回滚的管理出站能力，并可让指定的 systemd Agent 使用国外出口。它运行在生产 VPS 上，属于安全关键工具。

项目当前最重要的目标不是继续增加功能，而是保持生产环境上的运行安全与可回退，以真实使用中发现的问题驱动维护，并继续使用可重复、可审计、可回退的开发与发布流程。

## 正式来源与文档职责

- GitHub 仓库 [`Cr0ce11/po0-unlock-assistant-public`](https://github.com/Cr0ce11/po0-unlock-assistant-public) 的 `main` 是代码和文档的唯一正式来源。
- Pull Request 记录实现范围、风险、测试证据和恢复方式。
- GitHub Release 是可部署脚本的唯一正式来源；每个正式版只提供 `po0-unlock-v2.sh` 与 `po0-unlock-v2.sh.sha256` 两个附件。
- 服务器只用于部署和运行，不能作为代码或项目记录的唯一来源。

聊天可以用于讨论和确认，但长期有效的结论必须进入以上正式记录。

本仓库当前**没有启用 GitHub Issue**，因此需求、缺陷和优先级暂时由 [`TODO.md`](TODO.md) 配合 Pull Request 承载。如果项目所有者今后决定启用 Issue，应改为以 Issue 为主要登记入口，`TODO.md` 只保留索引。

v2.5.17 以前的历史不在本仓库内：原私有仓库与本地冷归档已在公开版接管和恢复能力验证完成后按项目所有者授权删除，不再是恢复来源；更早版本的结论以 [`CHANGELOG.md`](CHANGELOG.md) 中保留的产品变更摘要和公开 Release 为准。

每类长期信息只维护一个主要正式来源：

| 信息类型 | 主要正式来源 |
|---|---|
| 当前版本、环境、发布形态和维护执行方 | 本文件 `PROJECT.md` |
| 可执行事项、优先级和状态 | [`TODO.md`](TODO.md) |
| 执行规则、源码与产物边界、安全红线、本地验收清单 | [`AGENTS.md`](AGENTS.md) |
| 工作区级规则（GitHub 认证、文件布局与清理） | [`../AGENTS.md`](../AGENTS.md) |
| 每个版本相对上一正式版的变化 | [`CHANGELOG.md`](CHANGELOG.md) |
| 用户可见行为与操作步骤 | [`README.md`](README.md)、[`使用说明.md`](使用说明.md) |
| 维护交接基线与交回记录 | [`docs/HANDOVER.md`](docs/HANDOVER.md) |
| CI 与发布门禁的实际实现 | [`.github/workflows/ci-release.yml`](.github/workflows/ci-release.yml) |

其他文件可以提供摘要和链接，但不重复维护同一套操作步骤或门禁清单。

## 当前状态

| 项目 | 当前状态 |
|---|---|
| 最新正式版 | v2.5.23（2026-08-15） |
| 主开发分支 | `main` |
| 仓库形态 | 公开可读，未授予开源许可；Issue 未启用，协作入口只有 Pull Request |
| 运行位置 | 只在国外出口 VPS 上以 root 运行；国内入口组件只能由国外出口主控释放、传输、校验和调用 |
| CI 覆盖环境 | Debian 12（bookworm）与 Debian 13（trixie）容器矩阵 |
| 提交前门禁 | [`AGENTS.md`](AGENTS.md) 的《本地验收清单》，逐项执行且必须全部通过 |
| CI 门禁 | `ci-release.yml`：固定版本 ShellCheck 基线、版本号唯一性与标签一致性、单文件确定性重建、验收注册检查、`tests/*-acceptance.sh` 全部套件，Debian 双版本重复执行 |
| 发布形态 | 推送 `v*` 标签触发；先建草稿并上传两个附件，把 GitHub 返回的 digest 与本地 SHA-256 逐一核对后才发布；同标签 Release 已存在时拒绝 |
| 真机验证 | 本地与 CI 只覆盖语法、构建确定性和离线验收。项目所有者已决定（2026-08-14）不安排专门的验证机，由其在日后实际使用中验证，详见 `TODO.md` 的 OPS-001。已进入正式记录的真机结果：2026-08-15 由项目所有者在自有环境确认 v2.5.22 的 Agent 扫描恢复正常（该路径在 v2.5.21 上因 BUG-014 失败）；健康检查在 v2.5.20 上暴露的 BUG-012 也是同一渠道发现。任何真实服务器操作仍需项目所有者按次授权具体服务器 |
| 生产环境状态 | 未进入本仓库正式记录；本仓库的公开历史建立以来没有记录过登录或修改生产服务器的操作 |
| 已知未解决缺陷 | 无已登记项；2026-08-14 全项目审查确认的 22 条已全部处理或按所有者决定暂缓，剩余 SEC-004 待定方案 |
| 维护执行方 | 2026-08-14 起由 Claude Code 执行；2026-08-15 项目所有者决定暂不交回 Codex，维护期延长。基线、约束与延长记录见 [`docs/HANDOVER.md`](docs/HANDOVER.md) |

## 职责边界

- 项目所有者保留最终业务验收、生产环境上线、高风险操作授权，以及公开仓库设置的决定权（是否启用 Issue、是否授予开源许可、仓库可见性与保护规则）。
- AI 执行代理（Codex 或 Claude Code）负责：先读 [`AGENTS.md`](AGENTS.md) 与本文件，再实现代码、更新测试与文档、创建分支和 Pull Request，并在提交前逐项执行本地验收清单。两方遵守同一份规则，不建立任何代理专属的规则文件或流程。
- 可能造成服务中断、数据丢失、权限变更、凭据暴露、不可逆写入或影响生产环境的操作，必须在执行前准备好回退方式并取得项目所有者明确授权。

## 如何更新本文件

当最新正式版、仓库形态、CI 覆盖环境、发布形态、生产环境状态、维护执行方或职责发生变化时，必须在同一个 Pull Request 中更新本文件。具体任务和进度不要堆积在这里，记录到 [`TODO.md`](TODO.md)；版本变化记录到 [`CHANGELOG.md`](CHANGELOG.md)。
