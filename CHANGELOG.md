# 迭代记录

这里只记录每个版本相对上一正式版的变化。v2.5.16 及更早版本的条目作为产品变更摘要保留；对应的旧 Git 历史、Issue 和 Release 未迁入本公开仓库，原私有仓库及本地冷归档已在迁移验证完成后按项目所有者授权删除。

## 未发布

## v2.5.18（2026-08-14）

- 修复脱敏诊断对同行相邻 IPv4 地址只能隐藏部分内容的问题；自由文本中以空格或标点紧邻的多个地址现在会逐个隐藏。
- 单文件构建器现在会精确核对所有字面量改写点的匹配次数，并在生成后复核三个组件释放入口、空路径占位和内部测试分支；相关匹配或生成语义不符合预期时会明确中止，不再静默产出已知缺少内嵌组件初始化的脚本。
- CI 与本地验收新增同一套 ShellCheck warning 门禁；同时收紧六个循环变量的函数作用域并移除两处无效变量，防止后续 Bash 改动引入静默的全局变量污染。
- 国外出口调用正式国内入口组件时按操作类型加入 15 至 300 秒上限，超时会区分只读零修改与修改型可能部分完成；国内入口六处配置写锁统一改为最多等待 30 秒，超时后拒绝无锁写入并给出完整回滚指引。
- 更新公开迁移记录：旧私有仓库与本地冷归档已在公开版接管和恢复能力验证完成后按授权删除，不再作为项目恢复来源。

## v2.5.17（2026-08-13）

- 将已完成官方源码与生产二进制核对的 Go CF Probe v1.0.3 至 v1.0.6 加入精确修订白名单；快照版和其他未知修订版仍保持拒绝修改。
- 单文件构建器在生成结果与现有 `po0-unlock.sh` 完全相同时不再创建重复备份；内容变化时使用独立的受管备份命名，并只保留最近 10 份。旧格式历史快照不会被自动删除。
- 项目从全新、干净的 Git 历史建立公开仓库；原私有仓库继续保持私有并作为历史归档，旧标签、Issue、Pull Request、Actions 日志和 Release 不迁移。当前仓库公开可读但暂不授予开源许可。
- 项目从 v2.5.17 起只维护和发布一个公开版 `po0-unlock-v2.sh`，保留在线更新、上一版助手恢复及其他全部功能；更新器仅访问预期的公开 Release，不再要求、读取或保存 GitHub Token。
- 旧私有版和旧分享版都可由手动上传的更高公开版原子接管，失败时恢复原脚本且不改代理、隧道、Agent 或连接配置。旧私有版的 Token 文件须在接管确认后手工删除，并在 GitHub 端撤销；新版不读取或自动删除历史凭据。
- Release 工作流只发布脚本与对应 SHA-256 两个附件；先创建草稿并上传，把 GitHub 返回的附件 digest 与本地摘要全部核对后才发布。同标签 Release 拒绝复用，失败时只清理本次未发布草稿；Actions 默认只读且仅发布作业取得写权限。

## v2.5.16（2026-08-08）

- 功能 4 选择已经托管的 Agent 后，新增“检查并更新配置 / 撤销国外出口配置 / 返回”菜单；撤销只处理当前服务并复用原有事务回滚，不卸载 Agent，也不会先执行无关的出口探测。
- 移除哪吒 Agent 的专用识别、自签名证书兼容、配置改写、连接验证、事务恢复、交互入口和完整回滚分支；同时删除对应模块与验收套件。
- README、使用说明、模块文档、工程规则和 CI 均同步移除当前哪吒能力说明，并加入单服务撤销的交互验收。

## v2.5.15（2026-08-08）

- 将已完成源码核对的官方 Go CF Probe v1.0.1、v1.0.2 加入精确修订白名单；v1.0.0 继续支持，快照版、未知修订版仍拒绝修改。
- Go CF Probe 未通过识别时不再统一误报为缺少旧 Shell `ping` 回退：未知 Go 修订版与受支持版本的运行参数/配置安全校验失败现在分别显示准确原因。
- 默认四项备用测速目标调整为金华三所高校官方域名公开发布的权威 DNS TCP 53 节点；写入前仍逐项验证当前入口实际可达，失败时保持原配置。
- 新增 Go CF Probe 面板动态配置守卫：首次配置会在成功提示前自动收敛面板启动时的覆盖，后续配置文件变化也会只恢复四项 Po0 测速目标并重启一次 Agent；面板新配置版本、密钥和其他字段保持不变，停用或回滚时守卫一并撤销。

## v2.5.14（2026-08-07）

- 新增当前官方 Go CF Probe v1.0.0 的原生测速端口兼容：保留已有安全自定义目标，只把缺少端口或命中 80、443、8080、8443、8000、1080 的目标替换为连通性验证通过的国内直连 TCP 53 目标；未知 Go 修订版保持拒绝修改。
- Go Agent 配置改写采用 root 专用事务记录，保留密钥与其他配置字段；启用、刷新、服务重启失败、停用、完整回滚和首次事务中断恢复均已接入。停用时会在重启前恢复原目标，失败则重新应用安全目标并恢复服务代理。
- 文档明确区分旧 Shell 与 Go Agent：旧 Shell 继续使用 `ping` 回退和备用 HTTPS 端口直连上报；Go Agent 当前只修复延迟，面板来源地区仍可能显示国外出口。
- Agent 扫描的刷新提示改为按实际 Agent 模式说明结果，不再对 Go Agent 误报“真实地区上报已生效”。

## v2.5.13（2026-08-05）

- 国内、国外两端首次初始化现在会在任何安装写入前拒绝悬空符号链接、异常状态根目录、重复状态目录和残留隧道家目录；`ACTIVE` 改为在受保护目录内生成候选并原子提交。
- 国外出口的专用管理密钥目录、私钥和公钥现在必须满足 root 属主、安全权限、普通文件及单一硬链接要求；缺失公钥通过受保护候选原子补齐，并复核公私钥匹配，异常路径不再被跟随或覆盖。新公钥使用 `0644`，同时兼容旧版在严格 umask 下留下的安全 `0600` 公钥；公私钥匹配只比较算法与密钥数据，不会因不同 OpenSSH 版本附带的无关注释产生错误中止。
- 更新连接时以“新反向隧道稳定”为配置事务提交点：此前失败仍恢复旧 `hosts.conf`，此后的 Agent 刷新或最终检查失败会保留与新隧道一致的新配置，不再形成运行状态与磁盘配置错位；如果旧配置自动恢复失败，唯一事务备份会保留并显示准确路径。
- 首次安装现在会在添加管理密钥前和上传国内入口组件前两次只读检查入口占用；已有 `ACTIVE` 的国内入口会明确拒绝另一台国外出口，不再触碰现有 Agent、隧道或代理配置。每次新安装还会记录随机事务标识，失败清理只有在标识匹配时才能回滚国内入口，避免并发安装或错误目标误回滚其他部署。
- 新安装的国外出口隧道私钥和公钥现在会显式收紧为 `0600/0644`；完整回滚同时兼容早期或严格 umask 留下的 root 专用 `0600` 公钥，仍拒绝宽松权限、符号链接和额外硬链接，避免安全公钥导致回滚中断。

## v2.5.12（2026-08-05）

- 连接更新遇到服务器重装导致 SSH 主机密钥变化时，新增显式安全恢复向导：自动读取当前 `ed25519` 指纹作为候选值，展示旧/新指纹，要求用户先通过服务商控制台核验并输入精确的 `REPLACE`；`--yes`、其他输入或 EOF 均不能绕过核验。
- 主机密钥替换采用临时文件和原子移动，替换前的专用 `known_hosts` 自动以 0600 权限备份并显示路径；扫描失败、无法比较或用户取消时旧记录保持不变，正常流程不再要求手工执行 `ssh-keygen -R`。
- 修复 Agent 交互扫描被 `timeout` 独立进程组暂停的问题；现在选择 `0`、服务编号和后续确认都会正常传递，仍保留 60 秒总超时保护。

## v2.5.11（2026-08-05）

- 国外出口的安装标记、隧道配置、主机密钥、代理配置和回滚目标现在统一要求 root 所有的普通文件、预期权限和单一硬链接；符号链接、悬空链接、异常硬链接或异常目录会被拒绝，避免健康检查、重配置恢复和回滚误触外部路径。

## v2.5.10（2026-08-05）

- 反向隧道验证成功后的 4 个连接状态文件改为带备份的事务提交；任一替换失败会整体恢复旧值，进程中断时保留未完成事务备份，不再留下半新半旧的连接记录。

## v2.5.9（2026-08-05）

- Agent 服务扫描对 systemd 服务列表和逐项元数据读取设置有界超时；远端扫描组件整体最多运行 60 秒，缺少 `timeout` 或读取超时会停止本次扫描，不会继续修改服务。

## v2.5.8（2026-08-05）

- 主控安装、连接更新、健康检查、Agent 扫描和回滚现在共用 root 专用非阻塞操作锁；已有操作占用时不会建立国内入口会话或执行远端命令，锁目录、文件的符号链接和异常硬链接也会被拒绝。

## v2.5.7（2026-08-05）

- 国内入口正式组件更新前，拒绝符号链接、异常硬链接、错误属主或权限的既有目标；写入后再次复核目标安全属性、哈希和语法，异常时保留原文件并停止。

## v2.5.6（2026-08-05）

- 国外出口确认反向隧道远端监听时，显式只使用隧道专用 `known_hosts`，并屏蔽系统全局主机密钥，避免就绪探测绕过 Po0 已保存的主机密钥记录。

## v2.5.5（2026-08-05）

- 首次授权国内入口 SSH 时，改用 Po0 专用 `known_hosts` 和严格主机密钥策略；SSH 会先显示指纹供用户与服务商控制台核对，未确认或记录权限异常时不会发送密码、建立控制连接或执行远程命令。

## v2.5.4（2026-08-04）

- 更新现有连接配置时，先在内存中准备并展示新地址；用户确认前不会改写 `hosts.conf`。确认后的授权、隧道或最终状态检查失败时，会自动恢复原连接配置并清理事务临时文件，避免配置与现有隧道错位。

## v2.5.3（2026-08-04）

- 功能 4 会先复用经过属主、权限、硬链接数、SHA-256 与语法校验的已安装国内入口组件；只有组件不一致时才临时上传，并在连接与组件准备前显示进度和耗时。
- Agent 候选列表不再被无关的外网探测阻塞：只查看或输入 `0` 取消时不会请求公网；确认修改服务后仍会完整验证 HTTP 与 SOCKS5 出口，但两项改为并行执行，最坏等待由约 40 秒缩短为约 20 秒。
- 扫描运行中服务时一次读取每个 systemd 单元所需的描述、路径和启动命令，不再对每个服务重复执行三次元数据查询，并显示服务信息读取耗时。

## v2.5.2（2026-08-04）

- 同一次安装、连接更新、健康检查、诊断、Agent 扫描或完整回滚会复用一条受保护的临时 SSH 控制连接，SSH 与 SCP 不再为每个子步骤重复进行公网握手；操作结束或中断时会关闭会话并清理 root 专用临时目录。
- 只对不执行远程命令的初始控制连接进行最多 3 次有限重试，并采用 1 秒、2 秒退避；远程命令和文件上传仍各自只执行一次，避免链路抖动造成重复修改。
- 连接更新的最后状态检查会复用刚刚校验安装的国内入口组件，不再重复完整预检和上传；普通状态与健康检查会先核对已安装组件的属主、权限、硬链接数、SHA-256 和语法，完全一致时直接复用，否则仍使用临时上传的当前内嵌组件。
- 连接更新第 3 步拆分显示组件同步、隧道稳定、代理与 Agent 刷新、最终状态检查四个阶段及各自耗时；健康检查也会显示国内入口准备、执行和总耗时，公网慢链路不再长时间没有反馈。
- 脱敏诊断报告新增连接配置、两端概况、完整健康检查和近期错误采集耗时，并记录同一次操作实际发生的国内入口 SSH 初始建连次数，便于区分握手等待和远端执行耗时。
- 诊断报告会从受保护的安装状态读取当前隧道配置生效时间；状态路径、属主、权限、硬链接数或时间格式无法安全验证时只显示“未知”，有效时间转换为不会被脱敏规则误判的 UTC 紧凑格式。
- 健康检查、两端原始状态和组件参数错误统一使用“连接路径、连接地址、国外出口源地址”等公网/私网通用措辞；内部兼容字段和状态文件名保持不变。

## v2.5.1（2026-08-04）

- 更新连接配置时，仍优先使用国内入口服务商私网；如果国外出口没有可用于该路径的本机私网 IPv4，不再直接退出，而是请求填写国内入口公网 IPv4，然后按实际路由自动绑定国外出口源地址。
- 公网备用地址会拒绝私网、回环、链路本地和保留 IPv4；成功配置后继续使用原有兼容字段保存，不需要迁移旧版 `hosts.conf`。

## v2.4.5（2026-07-27）

- Agent 扫描与底层安全校验不再禁止 ForwardX；用户明确选择并再次确认后，可为 `forwardx-agent.service` 启用国外出口。ForwardX 使用不含 `ALL_PROXY` 的专用 HTTP/HTTPS 控制面代理配置，避免把原始 TCP 转发数据面整体改道，并继续由助手记录和回滚。

## v2.4.4（2026-07-26）

- 隧道重配置改为先连续验证新连接稳定运行，再提交新的连接状态；新单元启动、载入或稳定性检查失败时，会恢复并重新验证上一份隧道配置，避免一次失败更新中断原本可用的出站。
- 修复 CF Probe 验收中部分断言失败后仍可能报告通过的问题，并增加隧道延迟失败后的行为测试，确认旧单元、主机密钥与最后一次有效连接状态均被保留。
- 完整回滚不再要求尚未完成代理初始化的安装具备私网地址记录；删除受限隧道账户前会保存并复核 UID，不再依赖 `userdel -r` 递归清理，而是在确认路径、属主与挂载边界安全后单独删除家目录，失败时可安全重试。
- 旧版 Komari 延迟转发清理会显式检查服务停止、防火墙撤销、文件删除与 systemd 重载结果；服务或防火墙步骤失败时保留恢复脚本和配置，不再误报清理成功。
- 核心流程不再依赖不同发行版参数不一致的 `nc`：安装预检直接使用绑定私网地址的 SSH 验证，代理就绪状态由实际 HTTP/SOCKS 出口请求确认，健康检查改用 SSH 握手探测。
- 危险操作确认在输入结束或非交互标准输入关闭时会安全取消，不再重复输出错误并无限等待。
- 主菜单中的 Agent 扫描即使遇到中断、SSH 断线或远端错误，也会显示失败提示并返回菜单，允许用户稍后重试。
- 更新验收新增空文件和超限文件的完整行为测试，确认异常候选在备份与替换当前脚本之前即被大小闸门拒绝；失败退出时会同步清理下载临时文件。
- Komari 身份守卫新增运行时测试，覆盖成功、服务器错误、超时、非明确 401、明确未授权、守卫配置损坏和本地身份损坏，验证仅确定失效时才隔离并完整保留旧身份。
- CF Probe 与诊断验收改为在严格子进程中运行每个用例，并补齐遗漏的失败传播，避免前置断言失败后被后续成功覆盖成假绿。
- CF Probe 验收改用测试自带的确定性 `ping` 夹具，不再要求 CI 主机预装 `iputils-ping`。
- 验收注册守卫会自动发现全部套件，拒绝未注册、重复注册或没有定义的 `test_*` 测试，并确认每个套件同时接入基础验证和 Debian 12/13 兼容任务，避免手工清单遗漏后静默少测；菜单验收也不再因本地交互终端启用 ANSI 颜色而误报失败。
- 国内入口的专用隧道密钥采用最小转发权限：只允许监听两个项目回环端口，并把本地、动态和跳板连接限制到不可路由目的地；已安装的 v2.4.3 授权文件会在“更新连接配置”时原子迁移，检测到外部修改则保留原文件并停止。
- 国内入口在创建状态目录、`ACTIVE` 标记和隧道账户之前完成 Base64 解码、单行约束与 SSH 公钥格式验证；错误或多行公钥不会留下阻止重试的半安装状态。
- 主控、国内入口、国外出口和底层助手统一限制 IPv4 每段只能包含 1–3 位数字，再进行 0–255 范围判断；超长数字不能再通过整数回绕绕过校验或写入 Agent 配置。
- 新建和重配置反向隧道时，不再只观察 systemd 进程状态；助手会要求同一 SSH 主进程连续稳定，并通过严格校验的管理连接确认国内入口两个回环转发端口都已监听，失败时不写入新状态且重配置会恢复旧隧道。
- 启用哪吒自签名证书兼容时，最终提交会在同一失败即停止的操作链中复核候选哈希与目标原始哈希后再原子替换；配置或候选在提交前发生漂移时均保留现场并拒绝覆盖。
- 国外出口代理与反向隧道的临时配置改由保留原退出状态的 `EXIT` 陷阱清理，并覆盖中断信号；验证失败、`die`、部分创建失败和正常完成均不会在 `/tmp` 留下文件。
- 国外出口健康检查移除永远不可达的“仅提醒”返回码，明确只返回正常或异常；主控对其任何非零状态都按异常处理，同时保留国内入口真实使用的提醒汇总。
- 使用 `--yes` 启动时，正式入口交接以及菜单内更新或恢复后的脚本重入会继续保留无人值守模式，避免新进程重新出现确认等待。
- 国内入口经 SSH 返回的组件临时路径必须严格匹配预期目录、文件名前缀和 8 位字母数字后缀，通过验证后才会进入上传、执行或清理命令。
- GitHub 私有请求与令牌验证改为按变量名接收敏感值，并在隔离子进程内自行关闭命令跟踪；即使调用者启用了 `xtrace`，调用行和函数内部也不会展开令牌，返回后仍保留调用者原有调试状态。
- 恢复使用旧配置位置的助手版本时，会先保留新旧两份内容一致的配置，待脚本或旧入口提交成功后再清理新位置；恢复中途失败时，当前版本仍有可用配置，不会留下版本与配置位置错位。

## v2.4.3（2026-07-25）

- Agent 扫描增加 NyanPass 产品识别：官方默认 `nyanpass.service`，以及用户自定义服务名但描述仍为 `nyanpass` 的运行中服务，都会作为“NyanPass 转发面板 Agent”列出，并继续由用户确认是否启用国外出口。
- 不把未经证实的 `nypass` 当作别名，不隐藏或处理服务器没有 IPv6 时产生的连接日志，也不把 systemd 的 `active/running` 误报为面板控制连接已经建立。

## v2.4.2（2026-07-24）

- 正常脚本更新与恢复成功后自动整理旧脚本备份，最多保留 3 份，并始终保护当前可由菜单恢复的备份。
- 备份清理只处理严格命名且所有权安全的正常更新备份；发现符号链接、异常属主或硬链接时停止清理并保留全部文件，人工候选目录不在自动清理范围内。

## v2.4.1（2026-07-24）

- 完整回滚确认界面增加 `0` 返回主菜单；其他无效输入会留在当前界面重新提示，不再以“用户取消”错误退出助手。
- 保留仅恢复助手脚本、不修改服务的应急能力，并将菜单名称调整为“恢复上一版助手（不改服务）”，减少与完整业务回滚的混淆。

## v2.4.0（2026-07-24）

- 将长期连接配置从 `/root/hosts.conf` 迁移到标准位置 `/etc/po0-unlock/hosts.conf`，配置目录仅 root 可进入，配置文件继续使用 `0600` 权限。
- 现有部署首次运行新版时会验证、复制并核对配置，确认一致后自动清理 `/root/hosts.conf`；新旧配置内容不同时保留两者并停止，不擅自选择。
- 全新部署直接写入新位置；符号链接、错误属主、宽松权限和异常硬链接均被拒绝。
- 撤销到 v2.3.0 或更早版本前，自动把配置恢复到 `/root/hosts.conf`，保证旧版仍可正常读取；再次升级后可重新迁移。
- 新增配置迁移与回退专项验收，并纳入 Debian 12/13 持续检查。

## v2.3.0（2026-07-24）

- 一键安装完成后，将助手正式安装到 `/usr/local/sbin/po0-unlock`，并自动创建稳定的 `po0` 快捷命令；首次上传到 `/root` 的引导脚本随后可以安全删除。
- 现有 v2 部署首次运行新版时会自动补齐正式系统入口，并安全接管原先指向 `/root/po0-unlock.sh` 的人工 `po0` 快捷链接，再立即切换到正式脚本继续原操作；后续检查更新和撤销更新不再依赖 `/root` 中的上传文件。
- 将连接配置固定保存在 `/root/hosts.conf`，避免脚本移动后丢失原有连接信息。
- 系统入口或 `po0` 命令被其他文件占用时拒绝覆盖；脚本内部自检和组件提取不会创建或修改系统入口。
- 从 v2.3.0 撤销到更早版本时，会自动恢复旧版使用的 `/root` 脚本入口；旧版重新更新后再自动切回正式系统入口，保证跨入口升级可回退。
- 完整业务回滚仍保留助手入口，方便用户重新安装、检查状态或管理脚本版本。

## v2.2.1（2026-07-24）

- 优化一键健康检查排版：国外出口和国内入口分别按职责分组，每项改用“状态 + 检查项：说明”的单行格式，不再依赖中英文混排时不稳定的固定列宽。
- 移除两端重复标题，增加 `1/2`、`2/2` 进度标识，统一两端小结和总体结果区域；正常结果会明确说明无需修复且配置未被修改。
- 保持全部检测逻辑、检查顺序、退出状态、只读边界和安全修复行为不变，并增加跨终端排版契约测试。

## v2.2.0（2026-07-24）

- 将主菜单第 3 项整合为“健康检查与问题处理”，不新增一级菜单；健康检查发现问题且未解决时，可继续生成诊断报告。
- 新增 `po0 diagnose` 直接命令，只读汇总两台服务器的系统概况、核心健康状态、托管 Agent 数量和有上限的近期错误日志。
- 诊断报告只保存在国外出口的 root 专用目录，不会自动上传；生成过程使用受保护的临时文件和原子写入。
- 自动隐藏报告中的地址、域名、账号、端口、令牌、密码、密钥和指纹，并增加独立验收测试覆盖只读边界、权限保护、脱敏与日志上限。

## v2.1.0（2026-07-23）

- 将主菜单“查看运行状态”升级为面向普通用户的一键健康检查，汇总检查两端安装记录、核心服务、监听端口、私网路由、代理联网、托管 Agent 和已知旧组件残留。
- 检查阶段保持完全只读，不再遇到第一处异常就停止；结果统一显示“正常、提醒、异常”和整体结论。
- 发现核心故障时可由用户明确确认后执行安全修复；修复仅恢复本项目自有的国外出口代理、反向隧道服务和国内入口管理组件。
- 自动修复前验证文件归属和关键内容，不改地址、SSH、系统路由、Agent 或第三方配置；任一核心服务修复失败会恢复两个服务的原运行与开机启动状态。
- 保留 `raw-status` 高级排障命令，并新增独立验收测试覆盖只读边界、检查范围、用户确认和失败回滚。

## v2.0.1（2026-07-23）

- 移除会把 Komari TCP 建连时间错误缩短为 `0 ms` 的 redsocks 延迟转发方案；不再安装或启用该依赖。
- Komari 的控制连接和真实 IP 功能保持不变；延迟任务改为由用户在 Komari 面板选择 ICMP，直接显示国内入口的真实网络延迟。
- 更新已托管 Komari 时会验证所有权并清理旧版专用服务、配置和防火墙规则；无法确认软件包来源，因此保留已安装但未运行的 redsocks 软件包。
- 补充回归测试，确保新安装不再创建错误方案，旧安装可以安全清理且外部改动仍受保护。

## v2.0.0（2026-07-23）

- 建立不兼容 v1 的全新运行命名基线，统一使用 `po0-unlock`、`cn-entry` 和 `overseas-exit`。
- 重新命名状态目录、systemd 服务、隧道账户、SSH 密钥、配置文件、状态文件、辅助命令、组件文件和固定安装路径。
- 删除 v1 配置文件与运行状态兼容分支；v2 只允许在已经清理的环境中全新安装。
- 使用独立的 `po0-unlock-v2.sh` Release 资产，阻止 v1 更新器把 v2 当作原地升级。
- 增加活动源码旧名称清零检查，并同步更新确定性构建、Debian 12/13 CI 和用户文档。
- 实机修复完整回滚等待反向隧道退出时间过短、首次操作可能需要再次运行的问题；现在会在安全上限内等待连接自然结束。

## v1.2.5（2026-07-23）

- 修复 Agent 扫描把脚本自身的 Komari 延迟辅助服务误认为新 Komari Agent 的问题。
- 增加回归测试，确保内部辅助服务不会出现在用户选择列表，同时真正的 Komari Agent 仍可正常识别。

## v1.2.4（2026-07-23）

- 将国内入口组件按基础环境、Agent 专项兼容、服务事务、交互管理和回滚职责机械拆分为构建时模块。
- 新增国内入口模块确定性构建器，并由单文件构建、离线验收和 Debian 12/13 CI 共同检查生成文件一致性。
- 将原先限定具体城市的国内入口组件统一命名为 `cn-entry-role.sh`，适配 Po0 的不同国内地区产品。
- 为兼容现有安装，历史状态目录、systemd 服务名和状态文件名保持不变；除内部组件名称外，不改变功能、菜单、配置、权限或服务器行为。

## v1.2.3（2026-07-23）

- 修复直接命令或无交互终端执行脚本更新、恢复成功后，错误尝试打开主菜单的问题；从交互菜单操作时仍会正常切换到更新后的界面。
- 补齐 Komari 面板展示 IPv4 专项验收，覆盖公网地址校验、设置、更新、清除、连接配置刷新保留、停用与完整回滚清理。
- 将展示 IPv4 配置改为由可独立测试的确定性写入函数维护，不改变现有配置格式或服务器行为。
- 同步更新持续检查和发布说明，补上已经投入使用的 Komari 验收测试。

## v1.2.2（2026-07-23）

- 为使用 `--auto-discovery` 的 Komari Agent 增加启动前身份有效性守卫，避免复用已经失效的 `auto-discovery.json` 后永久无法重新注册。
- 守卫通过面板只读任务接口验证缓存令牌；只有本地身份文件损坏，或面板明确返回 Komari 的 `401 Unauthorized` JSON 时才隔离旧身份并让 Agent 重新自动发现。
- 网络、DNS、TLS、代理故障以及 `404`、`5xx` 等非明确失效响应均保留原身份，避免临时故障导致节点重复注册。
- 自动发现密钥和客户端令牌不会写入助手状态、systemd 配置或命令行；旧身份以 `0600` 权限保留在 root 专用备份目录。
- 新安装、旧版增量修复、连接配置刷新、停用、失败回滚与完整回滚均已接入身份守卫，并增加所有权与安全边界验收。

## v1.2.1（2026-07-23）

- 修复 Komari Agent 经国外出口联网后，面板下发的 TCP/HTTP 延迟探测仍直接连接、从而可能长期不显示延迟的问题。
- 检测到 Komari 时自动配置仅属于该 Agent 的延迟转发；公网 TCP 测速使用国外出口，私网、回环和系统内其他程序不受影响。
- 缺少 `redsocks` 时先用普通提示征求确认，再从 Debian 官方软件源安装；软件包自带的全局服务保持关闭。
- 已有托管 Komari 可通过再次扫描增量补齐；停用 Agent 国外出口或完整回滚时同步移除专用服务、配置和防火墙规则。
- 新增独立验收测试，并纳入 Debian 12、Debian 13 持续检查。

## v1.2.0（2026-07-22）

- CF Probe 经国外出口连接面板时，面板上报优先通过 Cloudflare 备用 HTTPS 端口从国内入口直接发出，使面板识别国内入口的真实来源地区。
- 不修改 CF Probe 配置文件或面板代码；Agent 自动更新、延迟检测及其他联网请求保持原有行为。
- 直连上报失败时自动退回国外出口，避免为了地区显示导致监控节点掉线。
- 已有 v1.1.6 兼容目录可增量升级，失败回滚与停用清理同步覆盖新增文件。
- 修复 CF Probe 验收脚本可能在内部断言失败后仍显示通过的问题。

## v1.1.6（2026-07-22）

- 修复 `cf-probe.service` 经国外出口联网后可能不显示延迟的问题。
- 不修改 CF Probe 本体，通过服务专用兼容设置使用 Agent 已有的 `ping` 回退，因此不影响自动更新和系统中的其他程序。
- 新安装时自动应用；现有已托管 CF Probe 可通过更新连接配置或再次扫描补齐。
- 停用服务国外出口、失败回滚和完整回滚时同步清理兼容文件。
- 增加 CF Probe 独立验收测试，并纳入 Debian 12、Debian 13 持续检查。
